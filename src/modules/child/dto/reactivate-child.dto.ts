/**
 * Body for POST /admin/children/:id/reactivate.
 * No fields required. Class exists for Swagger typing uniformity and to
 * reserve space for future fields (e.g. tariff_plan_id pre-select, note).
 */
export class ReactivateChildDto {}
