/**
 * Body for POST /admin/children/:id/activate.
 * No fields required. Class exists for Swagger typing uniformity and to
 * reserve space for future fields (e.g. enrollment note). Mirrors
 * `ReactivateChildDto`.
 */
export class ActivateChildDto {}
