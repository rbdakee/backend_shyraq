# Shyraq Backend v2 — Implementation Plan

> Source-of-truth для порядка реализации. Прогресс отмечается галочками сразу при завершении sub-task'а.

---

## 0. v2 baseline (P0–P5 + B5–B12 complete)

Репо `backend_shyraq_v2` — TypeORM + service.ts brocoders pattern. Достиг **B4-parity** на коммите `f1d6984` (P5 children & guardians).

| Фаза | Что сделано |
|---|---|
| **P0** Bootstrap | clone brocoders skeleton, удалено social/mail/files/sessions/roles/statuses/mongo/hygen |
| **P1** Foundation | shared-kernel (43 файла), `tenant-storage.ts` (AsyncLocalStorage), health module, initial migration с pgcrypto + uuid-ossp, eslint config настроен |
| **P2** Auth & Users | domain layer (3 entities + 11 errors), 5 TypeORM entities, миграция AuthAndUsersTables с RLS на `kindergartens` + `refresh_tokens`. Ports & adapters: SmsPort/MockSmsAdapter, JwtTokenPort/JsonwebtokenAdapter, PasswordHasherPort/BcryptPasswordHasher, OtpStorePort/RedisOtpStore, TokenBlocklistPort/RedisTokenBlocklist. Repos + mappers + TenantContextInterceptor + 4 guards + 5 decorators. AuthService (8 methods) + UsersService + 3 controllers. Two DB roles (`shyraq` superuser owns DDL; `shyraq_app` non-owner runs app); FORCE ROW LEVEL SECURITY включён. |
| **P3** Tenant Bootstrap | KindergartenModule (createKindergarten + first admin atomic TX, updateSettings, getMyKindergarten, inviteAdmin, listKindergartens, archive, restore). Минимальный StaffMember entity для admin seed. Миграция StaffAndKindergartenSettings. Cross-tenant phantom-row integration spec. |
| **P4** Organization | StaffService extended, GroupModule (rich aggregate с `assignMentor` инвариантом — partial-unique idx `idx_group_mentors_one_active WHERE unassigned_at IS NULL`), LocationModule, CameraModule. Миграция OrganizationTables. E2E объединены в один `organization.e2e-spec.ts`. |
| **P5** Children & Guardians | Child + ChildGuardian + child-group-history domain entities, 11 errors, миграция ChildrenAndGuardians (3 tables, FORCE RLS, partial-uniques `(child_id, user_id) WHERE status<>'revoked'` и `(kindergarten_id, iin) WHERE iin IS NOT NULL`, status/gender CHECK). ChildService ~700 строк (19 методов). ChildAccessGuard для admin/parent scope. NotificationPort + LoggingNotificationAdapter в SharedKernelModule. Контроллеры: child (admin), parent-child (read-only), parent-approval (state-machine + permissions). |
| **B5** Enrollment | EnrollmentModule (domain + persistence + service + controller + DTO + presenter), migration EnrollmentTables (2 tables + ENUM + FORCE RLS). State machine: new→in_processing→{waitlist\|card_created\|cancelled}, waitlist→in_processing, card_created→archive, cancelled→archive. transition→card_created reuses ChildService.createChild + inviteGuardian atomically via ambient TX. `src/shared-kernel/domain/errors/conflict.error.ts` base + DomainErrorFilter `ConflictError → 409` ветка. Tests: unit 42/412, integration 50/439, e2e 8/65. Swagger `/api/v1/admin/enrollments`. |
| **B6** Parent Onboarding | `POST /parent/children/link` (cross-tenant IIN search, creates pending guardian secondary\|nanny), `POST /parent/children/:id/unlink` (soft-revoke, 403 for primary). Auto-approve hook в `AuthService.verifyOtp` BEFORE assembleRoles via `ChildGuardianRepository.findPendingPrimaryByUserIdCrossTenant` (bypass_rls). 5 новых domain errors + shared-kernel `ForbiddenActionError` base (→ 403). DomainErrorFilter passes through `details` для `MultipleChildrenForIinError`. `ChildService` +155 строк (880 total). `parent-onboarding.e2e-spec.ts` 553 строки / 8 сценариев A–H. Нет новой миграции (схема P5 достаточна). Tests: unit 42/439, integration 52/473, e2e 9/73. |
| **B7** Schedule & Meal | Migration `1777556957492-B7ScheduleAndMeal.ts`: 6 tables (`schedule_templates`, `schedule_slots`, `schedule_week_snapshots`, `activity_events`, `meal_plans`, `meal_plan_items`) + ENUMs (`activity_event_status`, `meal_type`) + FORCE RLS + partial-uniques. ScheduleModule: rich aggregate `ScheduleTemplate` with slot-conflict invariant; `ActivityEvent` state machine `scheduled → in_progress → completed\|cancelled` (terminal states reject reverse); `schedule_week_snapshots` per-week immutable copies; admin/staff/parent controllers. MealModule: `meal_plans` daily with both partial-unique idx branches for `group_id` NULL/NOT-NULL; multilingual `dish_name jsonb`; admin/staff/parent controllers. `WeeklyRolloutService` + cron (`@nestjs/schedule` `0 23 * * 0` Asia/Almaty) idempotent via `existsAnyInRange` short-circuit (meal) + tryCreate snapshot-first ordering (schedule); manual-trigger super-admin endpoint `POST /admin/schedule/week-rollout/run`; RLS-scoped per-kg via `tenantStorage.run`. T7 (opus) code-review caught 2 CRITICAL TX-poisoning bugs before merge; fix-pass `b27b5dc` closed them. Note: commit `9c16743` mis-titled "meal module" (sub-agent message-race during parallel T3/T4 run) — content is ScheduleModule; acknowledged in §5. Tests: unit 49 suites/527 tests (13/56 skipped), integration 61/62 suites 582/583 tests (1 pre-existing flaky enrollment ordering check), e2e 73→90 (+17: schedule/meal/weekly-rollout). |
| **B8** Attendance & Timeline (manual) | Migration `1777588264314-B8AttendanceAndTimeline.ts`: 3 tables (`attendance_events`, `child_daily_status`, `timeline_entries`) + 4 ENUMs (`attendance_event_type`, `attendance_method`, `child_intraday_status`, `timeline_entry_type`) + FORCE RLS + `tenant_isolation` policy + unique idx `(child_id, date)` + indexes on `(kg_id, recorded_at DESC)` / `(child_id, recorded_at DESC)` / `(child_id, entry_time DESC)` / `(kg_id, date)`. AttendanceModule with `attendance.service.ts` (`checkIn`, `checkOut`, `patchEvent`, `setDailyStatus`, `listEventsByChild`, `listEventsByGroup`, `listEvents`, `listByKindergarten`, `listDailyStatuses`, `dashboardAttendanceToday`, `getDailyStatusByChildAndDate`, `getEventById`) and `timeline.service.ts` (`createEntry`, `updateEntry`, `deleteEntry`, `listByChild`). Atomic 3-table TX flow on check-in/out (event + timeline entry + daily_status upsert) inside ambient TX from `TenantContextInterceptor`. Daily status only auto-promotes `absent\|late → present` on check-in; `sick\|on_vacation\|early_pickup\|present` preserved. Check-out does not mutate daily status. `setDailyStatus` upserts unconditionally. Pickup validation on check-out via `ChildGuardianRepository.findApprovedActivePickupGuardian` (requires status='approved' + revoked_at IS NULL + can_pickup=true). Edit window for non-admin staff PATCH: `recorded_at::date == today` in `Asia/Almaty` (`// TODO(B22): per-kg configurable`). Admin PATCH bypasses window. Future-date guard (5min skew tolerance) on `recordedAt`/`entryTime` writes (`InvalidAttendanceTimestampError`, code `invalid_attendance_timestamp` → 422). Controllers: `staff-attendance.controller.ts` (check-in/check-out/PATCH), `staff-timeline.controller.ts` (CRUD + author check), `staff-daily-status.controller.ts` (POST), `admin-attendance.controller.ts` (list events / single / PATCH no-window / dashboard / list daily / `/admin/children/:id/timeline`), `parent-attendance.controller.ts` (timeline / attendance / daily-status — `ChildAccessGuard`). NotificationPort extended: 4 new methods (`notifyAttendanceCheckIn`, `notifyAttendanceCheckOut`, `notifyDailyStatusChanged`, `notifyTimelineEntryCreated`) + 4 event interfaces. `LoggingNotificationAdapter` implements all with `// TODO(B9): WS fanout` markers. Post-commit dispatch fire-and-forget on microtask (`// TODO(B9): real afterCommit / Outbox` for B9 WS-fanout safety). `ChildGuardianRepository.findApprovedActivePickupGuardian` added (port + relational impl). Daily-status repo `list(filter)` supports `groupId` (INNER JOIN children on current_group_id). T6 review (opus): 0 CRITICAL / 2 HIGH / 4 MEDIUM / 5 LOW. T7 fix-pass `05cddc2` closed H1 (kg-wide event listing without filters), H2 (groupId now plumbed through dashboard), M1 (parent daily-status `?date=` validated via DTO `@Matches`), M3 (future-date guard), M4 (cross-tenant daily_status e2e). M2 (post-commit microtask docstring) refreshed; real afterCommit/outbox deferred to B9. Tests: unit 49 suites/527 → **51/569**, e2e 12/90 → **14/111**, integration unchanged. |
| **B9** Notifications + WebSocket | Migration `1777627742228-B9NotificationsAndOutbox.ts`: 4 tables — `notification_outbox` (FORCE RLS, partial idx `idx_outbox_pending` on `(status, next_retry_at) WHERE status IN ('pending','failed')` for efficient polling), `notifications` (FORCE RLS, per-user history), `push_tokens` (global by `user_id`, no RLS), `notification_preferences` (global by `user_id`, no RLS). Outbox-pattern: business TX writes to `notification_outbox` atomically via `OutboxNotificationAdapter`; worker process drains every 2s via `OutboxPollerProcessor` with `SELECT … FOR UPDATE SKIP LOCKED`. `NotificationDispatcher` resolves recipients (guardians via `ChildGuardianRepository`, mentors via `GroupMentorRepository`, direct `user_id`), applies `notification_preferences` filter + nanny-policy (`role='nanny'` receives only `attendance.*` + `pickup.*`), fans out to `PushNotificationPort` (MockPushAdapter logs; FcmPushAdapter stub for B22) + WS rooms + `notifications` history row. `NotificationGateway` @ `/ws` (socket.io v4): handshake JWT auth via `auth.token` field, auto-subscribe `user:{id}` + `child:{cid}` (approved guardians cross-tenant) + `group:{gid}` (active mentors cross-tenant). `RedisIoAdapter` for cross-process pub/sub between api and worker. BullMQ: `OutboxPollerProcessor` (repeatable 2s), `WeeklyRolloutProcessor` (migrated from `@nestjs/schedule` — distributed-lock free via Redis). Process split: `npm run start:worker` runs `src/main.worker.ts` entry. REST: `POST/DELETE /push-tokens`, `GET /notifications` (cursor-paged), `POST /notifications/:id/read` + `/read-all`, `GET/PATCH /notifications/preferences`. T8 closed all `// TODO(B9)` sites: synchronous `await` inside ambient TX (`attendance.service.ts`, `timeline.service.ts`), `notifyGuardianApproved` in `autoApprovePendingPrimaries`, `notifyGuardianSelfRevoked` in self-revoke. T9 surprise: `OutboxPollerProcessor` was missing `tenantStorage` propagation — silently RLS-dropped notifications — fixed inline. T10 opus review: 1 CRITICAL (dispatcher DB error poisoning entire worker batch → duplicate delivery on retry) + 1 MEDIUM (preferences PATCH race + first-insert default leak) + 1 LOW + 8 OBS. T11 fix-pass `e704acd`: CRITICAL closed via per-event savepoint (TypeORM nested transaction = SAVEPOINT) in `OutboxPollerProcessor`; MEDIUM closed via single-statement `INSERT … ON CONFLICT DO UPDATE` with EXCLUDED merge for preferences. Tests: unit 51/569 → **56/636** (+67 tests, +5 suites); e2e 14/111 → **16/119** (+8 tests, +2 suites: `notifications.e2e-spec.ts` + `websocket.e2e-spec.ts`). Closed in `e704acd`. |
| **B10** | **Identity QR** | Migration `1777688591994-B10IdentityQr.ts`: 1 table (`user_qr_tokens`) + ENUM (`qr_purpose`) — NO RLS (cross-tenant by design; parent with children in multiple kindergartens uses one QR). Partial unique idx `(user_id, purpose) WHERE revoked_at IS NULL` (without `NOW()` predicate — PG rejects non-IMMUTABLE predicates on partial unique indexes; one-active invariant enforced in service code). IdentityQrModule: domain `QrToken` POJO (methods: `isActive()`, `isExpired(now)`, `shouldRefresh(now, threshold)`, `revoke(now)`, `recordScan(now)`) + 4 module errors (`QrTokenNotFoundError` → 404, `QrTokenExpiredError` → 410, `QrTokenRevokedError` → 410, `QrScanRateLimitExceededError` → 429). Two new shared-kernel base errors: `GoneError` (→ 410) + `TooManyRequestsError` (→ 429) added to `DomainErrorFilter`. Token model: 32-hex opaque (`crypto.randomBytes(16).toString('hex')`), SHA-256 hash stored in DB `token_hash` column, plaintext-only in Redis `qr:token:{plaintext}` TTL 24h. Redis cache port `QrTokenCachePort` + `RedisQrTokenCacheAdapter`; rate-limit port `QrScanRateLimiterPort` + `RedisQrScanRateLimiterAdapter` (`rl:qr:scan:{device_id}` INCR+EXPIRE 60). 3 endpoints: `GET /users/me/qr` (any authed role except `pending_role_select` — cross-tenant, lazy-issue, every GET issues fresh / server-side auto-refresh when expired or `< 1h` to expiry; advisory lock `pg_advisory_xact_lock(hashtext('qr:identity:'||userId))` prevents concurrent-GET 23505 race); `POST /staff/qr/scan` (`KindergartenScopeGuard` + `StaffOnly` + rate-limit 60/min/device — `device_id` validated against active refresh-token session; response includes `linkedChildren` kg-scoped to scanning staff's `kindergarten_id` via Variant A); `POST /admin/qr/revoke-all/:userId` (`AdminScope` — bulk DB revoke, Redis DEL not possible for all plaintexts so relies on DB-recheck on next scan; returns `{revoked_count}`). Cross-module wiring: `RefreshTokenRepository.hasActiveSessionForDevice` (new method, validates `device_id` against active refresh-token row), `ChildRepository.findByIdsCrossTenant` (new method, cross-tenant child lookup), `ChildGuardianRepository.findApprovedActiveByUserIdCrossTenant` (new method, cross-tenant guardian lookup for linked children). T6 opus code-review found: 3 CRITICAL (C1 X-Device-Id not plumbed through `/auth/otp/verify` + `/auth/refresh` + `/auth/role/select`; C2 concurrent `GET /users/me/qr` race condition — 23505 on unique constraint; C3 `linkedChildren` returned all kg's children, not scanning-kg-scoped). T7 closed C1 (commit `6f80bd0` — plumbed `device_id` header through OTP verify / refresh / selectRole response + helper removal), C2 (commit `f46a06a` — advisory lock + drop dead inner `dataSource.transaction()`), C3 (commit `75c27b0` — Variant A kg-scoped `linkedChildren` + new e2e Scenario K: parent with kids in both kgs → only scanning-kg's child returned + doc updates for H1/M4/L1). T7 also: advisory-lock integration race spec added (`f46a06a`). T7 finding: `selectRole` RLS bug for parent path (pre-existing, deferred to B22, tracked in §5 Active). Tests (post-T7): unit 59 suites (non-gated) / 730 tests; e2e **17 suites / 131 tests** (+1 suite +12 tests: `identity-qr.e2e-spec.ts` Scenarios A–K); integration green under `INTEGRATION_DB=1` (pre-existing `password authentication failed` env failures tolerated, same as B9). All commits independently green. **T7-2 (codex second review) closed 4 additional findings: HIGH#1 reuse-flow restored (paired Redis key `qr:user:{userId}:identity → plaintext`, TTL = token TTL; reuse when active row + plaintext present + expires_at-now > 1h; mint+revoke otherwise; admin revoke-all also DELs user-keyed cache), HIGH#2 RLS bypass leak removed from `RefreshTokenRelationalRepository.hasActiveSessionForDevice` (`SET LOCAL app.bypass_rls='true'` leaked into ambient request TX via PG SET LOCAL semantics; plain query through `manager()` passes RLS naturally), MEDIUM#1 admin revoke-all kg-scoped (target user must be active staff_member of caller's kg OR approved+active child_guardian for child in caller's kg, else 403 `user_no_relationship_to_kg`; missing user → 404 `user_not_found`), MEDIUM#2 advisory-lock moved to `IdentityQrRepository.acquireUserAdvisoryLock` port method (service no longer imports `tenantStorage` or runs raw SQL — CLAUDE.md §8 boundary restored). Final test counts: unit 736 / e2e 134 / race-integration 1.** |
| **B11** Pickup OTP & Trusted | Migration `1777788800000-B11PickupAndTrusted.ts`: 2 tables (`trusted_people`, `pickup_requests`) + ENUM `pickup_request_status` + FORCE RLS + tenant_isolation + indexes (`(child_id, is_active)`, `(kindergarten_id, child_id)`, `(kindergarten_id, status)`, `(child_id, created_at DESC)`); FK `attendance_events.pickup_request_id → pickup_requests.id` added as ALTER (B8 created column without FK); `attendance_method` ENUM already had `otp_pickup` (B8). PickupModule: `TrustedPerson` + `PickupRequest` POJO domain (state machine `otp_sent → {validated, expired, cancelled}`, methods: `validate(staffMemberId, attendanceEventId, now)`, `cancel(now)`, `expire(now)`, `revoke(now)`, `markUsed(now, deactivate)`) + 7 module errors via shared-kernel base classes (`NotFoundError`/`GoneError`/`ConflictError`/`ForbiddenActionError`). Ports: `PickupOtpStorePort` (Redis `otp:pickup:{requestId}` TTL 1800s + standalone attempts counter `otp:pickup:{id}:attempts` INCR atomic, `RedisPickupOtpStoreAdapter`); advisory lock port `PickupRequestRepository.acquireValidateAdvisoryLock` (CLAUDE.md §8 boundary maintained). Services: `pickup-request.service.ts` (createByStaff/createByParent/sendOtp/validateOtp/cancel/list/get) + `trusted-person.service.ts` (parent CRUD list/add/update/revoke). 3 controllers: `/staff/pickup-requests/*`, `/parent/children/:id/{trusted-people,pickup-requests}`, `/parent/trusted-people/:id`. ValidateOtp ordering: status/expiry → isLocked → rate-limit → readCode/match → attendance.checkOut (ambient TX, method='otp_pickup') → pickupRequests.update(validated) → trustedPeople.markUsed if applicable → clearCode → notifyPickupValidated. Trusted-people: `is_one_time=true` rows auto-deactivate on first use. Cross-module: `attendance.service.checkOut` widened with optional `pickupRequestId`/`method`; `NotificationDispatcher` event templates `pickup.otp_sent`/`pickup.validated`; nanny-policy whitelist includes `pickup.*`; SMS template `pickupOtpTemplate(code, childName, kgName)`. OTP budget: auth `OtpStorePort` per-phone reused (single rate-limit quota for login + pickup). T6 opus code-review: 1 CRITICAL (C1 route shadow `StaffController @Get(':id')` under `staff/` intercepted `/staff/pickup-requests`), 5 HIGH, 6 MEDIUM, 3 LOW, 4 OBS. T7 fix-pass `61a6853` closed C1 (StaffController moved `staff` → `admin/staff`, aligning with endpoints.md §2.2 SoT), H1 (real PG-backed race spec), H2 (dropped inner dataSource wrapper), H3 (cancel acquires advisory lock + `update({expectedStatus})` defense), H5 (unconditional clearCode), M2/M3/M4/M5/M6. H4 (phone PII on staff list) deferred to §5 Active. T7-2 second-opinion (opus fallback — codex 1.0.4 default model gpt-5.5 unavailable, CLI upgrade cache-locked): 0 CRITICAL, 0 HIGH, 4 MEDIUM, 3 LOW, 4 OBS. T7-3 fix-pass `795bf9c` closed all 7: NM1 `TrustedPerson.revoke` now throws `TrustedPersonRevokedError` (410) for already-revoked + not-active guards; NM2 `TrustedPersonService.update` guards revoked rows (closes backdating-history attack); NM3 `ListPickupRequestsQueryDto` for `GET /staff/pickup-requests` (was bare `@Query` typed as TS-erased enum → PG error 500 on invalid status); NM4 endpoints.md §3.6 validate-otp response shape synced; NL1 dropped unnecessary `forwardRef(() => AttendanceModule)`; NL2 Swagger `@ApiUnprocessableEntityResponse` → `@ApiBadRequestResponse`; NL3 endpoints.md §3.6 error map synced (`invalid_otp` → 400, `otp_rate_limit` → 429). 4 OBS deferred (B22 polish). Test counts: unit 736→821 (+85); e2e 134→147 (+13: `pickup.e2e-spec.ts` Scenarios A–L, `organization.e2e-spec.ts` paths updated for C1 fix); race-integration 1→2 (`pickup.race.integration.spec.ts`). Commit set T0 `5e47ed9` → T7-3 `795bf9c`. **B11 demo-ready — BP §7 closed.** |

| **Post-B11 Security Review** (`db8cb72`) | External review #3+#4 — 24 fixes across auth/RLS/state-machine races/WS/privacy. Round-1 (F1-F19 subset): refresh-token tenant-binding, /auth/role/select claim guard, blocklist on refresh, super-admin login rate-limit, REVOKE TRUNCATE migration, startup SUPERUSER probe, ChildAccessGuard sets req.tenant, RolesGuard on staff/meal-plans, GroupService mentor role check, staff deactivate cascade on mentor rows, conditional UPDATE WHERE status=expected (schedule transitions), advisory lock QR revokeAllByUser, kg-scoped QR scan, WS per-socket exp + Redis blocklist disconnect, OTP_TEST_PHONES prod-disabled, atomic Redis INCR+EXPIRE pipeline. Round-2 (#3-#13): parent-link rate-limit + payload minimization, primary-cannot-self-revoke, has_approval_rights cap≤2 advisory lock, enrollment card_created conditional UPDATE, meal week-copy advisory lock, check-in conditional UPDATE, /saas/auth/logout RolesGuard, revokeAllByUserId fresh-connection cross-tenant logout. 6 new race-integration specs. Tests: **unit 888 / integration 961** (+1 pre-existing flake deferred B22) **/ e2e 155** — all green. Closed `db8cb72` 2026-05-04. |

**Текущее состояние (Post-B18):** unit 1654 (+212 from B18) / integration 1521 (+1 pre-existing flake: `enrollment.service.integration.spec.ts:323` identical-timestamp ordering, deferred B22; integration delta for B18 TBD post-B17 push merge) / e2e 235 (+19 from B18: Scenarios A-K + L/M/N) — all green. Build + lint: green. B13 (Billing & Invoices) — closed. B16 (Custom Discounts) — closed. B18 (Diagnostics & Progress) — closed. **Phase A continues: B17 (Content) ⊥ B18 (Diagnostics) — both demo-ready. B21 (Lifecycle) requires B13 (already done). B22 (Polish) — финальная фаза.**

| **B17** Content & Stories (`8abeed2` → `14fa714`) | Migration `1777890002000-B17ContentAndStories.ts`: 2 tables (`content_posts` + `group_stories`) + FORCE RLS + tenant_isolation policy + REVOKE TRUNCATE + indexes (type/published_at/kg_id/group_id/created_at) + CHECK constraints. **Provider-agnostic:** `FileStoragePort` + `LocalFileStorageAdapter` + env-switch `FILE_STORAGE_PROVIDER=local|s3|yandex` (default local; real S3/Yandex deferred Phase B). Domain: `ContentPost` (state machine `draft → scheduled → published`, forward-only, `published` terminal — `transitionTo` throws `ContentPostInvalidTransitionError` on reverse) + `GroupStory` (24h TTL invariant in constructor — throws `StoryAlreadyExpiredError` if `expiresAt <= now`) + 7 module errors via shared-kernel base (`ContentPostNotFoundError`/`ContentPostInvalidTransitionError`/`ContentAccessForbiddenError`/`StoryNotFoundError`/`StoryAlreadyExpiredError`/`StoryAccessForbiddenError`/`MentorNotAssignedToGroupError` → 403). 4 services: `ContentService` (create/update/publish/schedule/delete/list/get — admin/staff/parent CRUD), `StoryService` (create/view/list/delete — mentor-authored, group-scoped, 24h TTL), `BirthdayGeneratorService` (creates `birthday` type ContentPost at 07:00 Asia/Almaty daily — per-(kg, child, date) advisory lock via `pg_advisory_xact_lock` prevents dedup races), `ContentFeedService` (aggregates news/schedule_pub/menu_pub/birthday content across groups for parent feed). 3 BullMQ processors: `ContentPublishProcessor` (repeatable every 5min — promotes `scheduled → published` for posts with `publish_at <= now`), `BirthdayGenerationProcessor` (`0 7 * * *` Asia/Almaty daily), `StoryCleanupProcessor` (hourly — soft-expires stories past TTL). Cross-module: `NotificationPort` +4 methods (`notifyContentPublished`, `notifyStoryCreated`, `notifyBirthdayContent`, `notifyMenuPublished`); `NotificationDispatcher` +4 EVENT_TEMPLATES + +4 RECIPIENT_RESOLVERS (`content.published` → resolveByGroup, `story.created` → resolveByGroup, `birthday.content` → resolveByKindergarten, `menu.published` → resolveByGroup); `OutboxNotificationAdapter` +4 method overrides; auxiliary repo methods added: `ChildGuardianRepository.findApprovedUserIdsByGroup` + `ChildGuardianRepository.findApprovedUserIdsByKindergarten` + `ChildRepository.listActiveByBirthdayMonthDay` + `GroupRepository.isUserActiveMentorForGroup`. 4 controllers: `AdminContentController` (~7 endpoints — CRUD + state transitions + file upload via `FilesInterceptor`), `StaffStoriesController` (~4 endpoints — mentor-scoped CRUD + view increment), `ParentContentController` (~2 endpoints — feed + get single), `SaasContentController` (~3 endpoints — super-admin CRUD across tenants). Total ~16 endpoints, multipart via `FilesInterceptor`. `AppModule`: `ServeStaticModule.forRoot` (serves `uploads/` at `/static/`) + `ContentModule` registered; new deps `@nestjs/serve-static` + `@types/multer`. T5 fix-pass (commit `701fd2f`) closed 6 bugs surfaced during e2e activation: entity `.typeorm.entity.ts` naming glob mismatch, `group_mentors` INSERT shape, PATCH undefined-leak on omitted fields, `transitionStatus` query result unwrap, birthday Asia/Almaty TZ locale, test error assertion shape. T6 (opus) review: 0 CRITICAL / 3 HIGH / 8 MEDIUM / 6 LOW / 3 OBS. T7 (codex gpt-5.4) review: 0 CRITICAL / 5 HIGH / 2 MEDIUM / 1 LOW / 0 OBS — 3 NEW + 5 confirms of T6. T8 fix-pass `25404e3` + lint follow-up `14fa714` closed all 5 HIGH (PATCH controller field clobber on undefined, file_upload_error doc drift, mentor cross-group story create, mentor cross-group story list, birthday dedup race) + 3 MEDIUM (parent `/staff/stories/:id/view` scope leak, MIME-aware multipart cap, e2e mojibake re-encoded) + new error `MentorNotAssignedToGroupError` (403 `mentor_not_assigned_to_group`) + per-(kg, child, date) advisory lock for birthday gen. Tests: unit 1714 (baseline 1705, +9 new T8 specs); content e2e 15/15; full e2e regression 250/252 (2 pre-existing skips, none B17-related). Carry-forwards: T6 MEDIUM#5/6/8/11 + LOW#12-17 + OBS#18/20 deferred B22 (see §5 Active). **B17 demo-ready — BP §9 closed.** |

| **B13** Billing & Invoices (mock provider) | Migration `1777886401000-B13BillingAndInvoices.ts` + follow-up `1777886402000-B13BillingFkFix.ts`: 8 tables (`tariff_plans`, `tariff_assignments`, `payment_accounts`, `invoices`, `invoice_line_items`, `payments`, `refunds`, `kindergarten_holidays`) + 6 ENUMs (`payment_type`, `payment_status`, `payment_status_v2`, `refund_status`, `tariff_type`, `tariff_applies_to`) + FORCE RLS + tenant_isolation policy + REVOKE TRUNCATE + FK ALTER `parent_requests.invoice_id → invoices.id` (B12 leftover closed). FK-fix: `tariff_assignments.assigned_by` and `refunds.processed_by` retargeted from `staff_members(id)` to `users(id)` (controllers write `req.user.sub` = users.id). BillingModule: 8 POJO domain entities (`Invoice`/`Payment`/`Refund` rich-aggregate state machines + 5 CRUD POJOs) + 16 errors via shared-kernel base + state-machine specs. **Provider-agnostic ports + Mock adapters + env-switch:** `PaymentProviderPort` + `MockPaymentProvider` + `HalykPaymentProvider` stub (B14), `DiscountEnginePort` + `MockDiscountEngine` (sibling+prepay only; B16 expansion), `FiscalReceiptPort` + `MockFiscalReceiptAdapter` (B15 real OFD). Env-switch via `useFactory`: `PAYMENT_PROVIDER=mock\|halyk\|...` + `FISCAL_PROVIDER=mock\|...` (default `mock`). Services: `InvoiceService` (createOneOff, manualMarkPaid с синтетическим Payment row provider='cash', cancel, generateMonthly + advisory-lock + `existsMonthlyForPeriod` short-circuit, generateFirstInvoice, generateLatePickupInvoice, prepayInvoice + DiscountEngine, buildPaymentCalendar Asia/Almaty), `PaymentService` (initiate с idempotency_key UNIQUE 23505 → `PaymentIdempotencyConflictError`, processWebhook с cross-tenant lookup + advisory-lock + per-event SAVEPOINT, markFailed), `RefundService` (create/approve/reject/process atomic с advisory-lock); `TariffPlanService`, `TariffAssignmentService` (cross-tenant tariff_plan validation + advisory-lock), `HolidayService`, `PaymentAccountService`. **Conditional UPDATE WHERE status=expected RETURNING *** для всех state-flips. **MonthlyBillingProcessor** (BullMQ repeatable, `0 2 1 * *` Asia/Almaty, `existsMonthlyForPeriod` short-circuit + advisory-lock per (kg, period); manual-trigger `POST /saas/billing/monthly-run`). **Cross-module hooks:** `enrollment.service.transition()` → `generateFirstInvoice` on `card_created` (LAX MODE — catches `TariffAssignmentNotFoundError` + warn-log + emits `enrollment.first_invoice_skipped` outbox event, kept in same TX для всех остальных ошибок); `parent-request.service.accept()` → `generateLatePickupInvoice` + `parent_requests.invoice_id` linkage on `late_pickup` (STRICT MODE; B12 leftover closed). **Outbox notifications** (8 new event keys): `invoice.created`/`paid`/`overdue`/`cancelled`, `payment.completed`/`failed`/`refunded`, `refund.processed`. Nanny-policy allow-list excludes billing events (`attendance.*` + `pickup.*` only allowed for nanny). Fiscal-receipt emit hook в `PaymentService.applyCompletedPayment` (try/catch — failure is non-fatal, `// TODO(B15)`). **Money precision:** `src/shared-kernel/domain/money.ts` `roundKzt` + `addKzt`/`subKzt`/`mulKzt`/`divKzt` helpers (B22 will migrate to Decimal-backed VO). **Controllers (11):** 7 admin (`AdminTariffPlanController`, `AdminTariffAssignmentController`, `AdminInvoiceController`, `AdminPaymentController`, `AdminRefundController`, `AdminHolidayController`, `AdminFiscalReceiptController` read-only B13 stub) + 1 super-admin (`SaasBillingController` POST `/saas/billing/monthly-run`) + 2 parent (`ParentInvoiceController` list/get/calendar, `ParentPaymentController` pay/prepayment) + 1 webhook (`PaymentWebhookController` POST `/webhooks/payments/:provider` no-auth, 200-always, NestJS `rawBody:true` for HMAC). ~35 endpoints, snake_case wire, full Swagger, `@ApiResponse` для 200/201/400/401/403/404/409/422/429. **Permissions (BP §4.13):** primary/secondary/nanny gate в parent endpoints — nanny → 403 `nanny_cannot_view_invoice`/`nanny_cannot_pay`; secondary respects toggleable `permissions.pay_invoices`. **DTOs (~25)** snake_case wire + 6 presenters. **T9 opus code review:** 2 CRITICAL / 6 HIGH / 8 MEDIUM / 8 LOW / 7 OBS. **T10 codex second-opinion:** +2 HIGH / +2 MEDIUM / +2 LOW / +3 OBS (focused on money precision, cross-tenant FK, payment-calendar UTC bug). **T11 fix-pass `df1c9af`** closed 3 CRITICAL (existsMonthlyForPeriod filter, refund coverage check, manualMarkPaid Payment row promotion) + 8 HIGH (refund advisory lock + Mock idempotency, due_date_from plumbed, TariffAssignment advisory lock, webhook rawBody via NestJS `rawBody:true`, PaymentProviderError → 502 sanitized, enrollment lax-mode skip-event, roundKzt money helper, cross-tenant tariff_plan validation). MEDIUM/LOW carry-forward в §5 Active (см. ниже). **Tests:** unit 980 → 1301 (+321 в B13), integration 1053 → 1390 (+337 incl. billing-cross-tenant 9 phantom + 9 race scenarios), e2e ~170 → 196 (+26 — billing.e2e Scenarios A–Y 25/27 + enrollment hook + parent-request late_pickup). 2 e2e scenarios skipped (S refund-reject — endpoint deferred to B22 polish). **B13 demo-ready — BP §4 closed.** Commit ladder: `da84db6` (T0 doc-sync) → `702718c`/`6a03e2f` (T1 migration + phantom) → `96a9a9b` (T2 domain) → `1a5cc45` (T3 ports + adapters) → `8ce82c3` (T4a services + repos + entities) → `6740873` (T4b cron) → `c376396` (T4c hooks) → `7e1bf48` (T5a payment) → `76b72af` (T5b refund) → `54c1062` (T5c fiscal + outbox + nanny) → `b368a8e` (T6 DTOs) → `7ed087d` (T7a admin controllers) → `b5b285a` (T7b parent + webhook controllers) → `b796326` (T8 fix-pass — FK / DomainErrorFilter / webhook body / BullModule) → `d426c8b` (T8 e2e) → `558fc4e` (T8 e2e regression fix — enrollment lax mode) → `971dc46` (integration spec FK fix) → `df1c9af` (T11 fix-pass — 3 CRITICAL + 8 HIGH closed). |

| **B18** Diagnostics & Progress (`8abeed2` → `f478470`) | Migration `1777890003000-B18DiagnosticsAndProgress.ts`: 3 tables (`diagnostic_templates`, `diagnostic_entries`, `progress_notes`) + FORCE RLS + tenant_isolation policy + REVOKE TRUNCATE + indexes (specialist_type, kg_id, child_id, assessment_date) + CHECK constraints + `validateTemplateSchemaShape` + `validateEntryData` (7 field types: text/number/boolean/date/select/multi_select/scale). DiagnosticsModule: 3 POJO domain entities (`DiagnosticTemplate` + `DiagnosticEntry` + `ProgressNote`) + 10 module errors via shared-kernel base. Schema validators: `validateTemplateSchemaShape` (sections → fields structure) + `validateEntryData` (runtime data vs schema). Services: `DiagnosticTemplateService` (create/update/activate/deactivate/list/getById — admin CRUD + staff read-only), `DiagnosticEntryService` (create/update/list/getById — specialist-authored, re-validates data against template.schema on PATCH), `ProgressNoteService` (create/update/delete/list/getById — mentor-authored with author-only guard), `MyTodosService` (6-month Asia/Almaty staleness algorithm via `findLatestPerActiveChildBySpecialistType` single-roundtrip DISTINCT ON query). Cross-module: `NotificationPort` +2 methods (`notifyDiagnosticNew`, `notifyProgressNoteNew`); `NotificationDispatcher` EVENT_TEMPLATES + RECIPIENT_RESOLVERS +2 (`diagnostic.new`, `progress_note.new` → `resolveByChildGuardians`; excluded from `NANNY_ALLOWED_EVENT_KEYS`). Cross-tenant child ownership guard: `assertActiveChildInKg(kgId, childId)` on create paths for both entries and progress notes (T7 HIGH fixes). Controllers (6): `AdminDiagnosticTemplateController` (admin CRUD + activate/deactivate); `StaffDiagnosticTemplateController` (read-only, specialist-type scoped); `StaffDiagnosticEntryController` (specialist CRUD, filter: child/specialist/template/date range); `MyTodosController` (`GET /staff/my-todos`); `StaffProgressNoteController` (mentor CRUD); `ParentDiagnosticController` (`view_diagnostics` permission gate — nanny excluded via `NannyNoDiagnosticsAccessError`). 19 endpoints total. Reviews: T6 (opus) — 0 CRITICAL / 2 HIGH (`@Transform` boolean mangles omitted params, 6-month cutoff month-end overflow) / 7 MEDIUM / 7 LOW / 7 OBS. T7 (codex) — +2 HIGH (cross-tenant child reference in `diagnostic_entries` + `progress_notes` — `assertActiveChildInKg` missing on create) + 1 MEDIUM (Child module boundary leak). T8 fix-pass `f478470` closed 4 HIGH + 4 MEDIUM + 1 LOW (boolean transform clamped; 6-month overflow fixed; cross-tenant child guard added; M2 specialist/mentor filter scoped to caller for non-admin; M3 `NannyNoDiagnosticsAccessError` thrown; M5 mentor `all` vs `specialist_type` filter; L6 Swagger schema example fixed). Integration cross-tenant phantom: 3 tables × 3 modes = 9 isolation cases. Tests: unit 1442→1654 (+212), e2e 216→235 (+19: Scenarios A–N), integration delta TBD post-B17 push merge. Commit ladder: `8abeed2` (joint T0 docs w/B17) → `baf608b` (T1 migration + phantom) → `3554b01` (T2 domain) → `a89e884` (T3 services + cross-module) → `1796781` (T4 DTOs + controllers) → `7e11ab0` (T5 e2e Scenarios A–N) → `f478470` (T8 fix-pass). **B18 demo-ready — BP §8 closed.** |

| **B16** Custom Discounts (`53f17c4` → `8c2b094`) | Migration `1777890000000-B16CustomDiscounts.ts` + follow-up `1777890001000-B16CustomDiscountsFkFix.ts`: 2 tables (`custom_discounts`, `custom_discount_applications`) + 2 ENUMs (`custom_discount_status`: draft/active/paused/expired/cancelled; `custom_discount_type`: percentage/fixed_amount) + FORCE RLS + tenant_isolation policy + REVOKE TRUNCATE + 3 indexes (incl. partial `idx_custom_discounts_active` for fast active-query in monthly cron + applications log indexes) + 4 CHECK constraints + `update_updated_at_column()` trigger function. FK-fix: `custom_discounts.created_by` retargeted from `staff_members(id)` to `users(id)` (controllers write `req.user.sub` = users.id — same root-cause as B13 FK-fix). DiscountEnginePort extended: `DiscountEvaluationInput.context` gains `customDiscounts` (snapshots) + `childContext` + `familyContext`. `DiscountEvaluationResult` gains `customApplicationsToWrite[]` + absolute-amount field `customDiscountAmount` (no rounding loss via percentage round-trip — B16 SO-1 fix). MockDiscountEngine handles custom rules: stacking algorithm (NS=non-stackable gates further stacking; contiguous stackable prefix wins when first match is stackable; otherwise top NS wins outright), priority-DESC + createdAt-ASC sort, percentage/fixed_amount amount calc, per-discount audit ledger output. Domain: `CustomDiscount` aggregate (POJO) with state machine `draft → active → {paused → active|cancelled, expired, cancelled}` (conditional UPDATE WHERE status=expected RETURNING *) + `CustomDiscountApplication` POJO (audit ledger). 7 module errors via shared-kernel base. Conditions evaluator pure function `evaluateConditions(conditions, ctx)` — 10 condition types + `all_of`/`any_of` composition (depth limit 3) + `validateConditionsSchema()`. Service `CustomDiscountService` 10 methods (create/update/activate/pause/resume/cancel/expireOverdue/list/getById/listApplications). Activation acquires `pg_advisory_xact_lock(hashtext('discount:activation:'||kgId||':'||discountId))` via repo port method + emits outbox event `discount.activated` atomic in same TX. `DiscountExpireProcessor` (BullMQ repeatable, `0 3 * * *` Asia/Almaty): bulk UPDATE `markExpiredBatch` idempotent. InvoiceService extended: 4 generation paths load active custom discounts → filter by targeting + per-child cap (advisory-lock `pg_advisory_xact_lock(hashtext('discount:apply:'||childId||':'||discountId))`) + total_max_uses cap → pass to engine → write `custom_discount_applications` + `incrementUsedCount`. `AdminCustomDiscountController` (9 endpoints, @Roles('admin') + KindergartenScopeGuard). Custom `@IsValidConditions()` decorator for DTO-level conditions JSONB validation. Key decisions: advisory-lock over unique-partial-idx for max_uses_per_child race (generalizes to cap>1); absolute-amount not percentage for money precision; stacking algorithm with NS-gates-further-stacking semantics. Closes B13 LOW#2: `InvoiceService.siblingsCount` now wired via `familyContext.siblingsInKgCount`. Tests: unit 1301→1442 (+141), integration 1390→1521 (+131), e2e 196→216 (+20: `custom-discounts.e2e-spec.ts` Scenarios A–O). Commit ladder: `53f17c4` (T0 docs) → `89bd16d` (T1 migration) → `96c8d09` (T2 domain) → `2d7d133` (T3 service) → `879ff33` (T4 DTOs/controllers) → `d9049fc` (T5 e2e + FK-fix migration) → `8c2b094` (T8 fix-pass). Reviews: T6 (opus) — 0 CRITICAL / 3 HIGH / 3 MEDIUM / 4 LOW / 5 OBS; T7 (codex gpt-5.4) — +1 HIGH (money precision) + 1 LOW. T8 fix-pass closed 4 HIGH + 2 MEDIUM + 1 LOW. Deferred: H2 audit-row mismatch / M1 default-no-op repos / L1 down() trigger / L2 roundKzt consistency / L4 REVOKE restore / SO-2 Scenario M BullMQ / OBS-1..5 e2e gaps (all B22). **B16 demo-ready — BP §4.1 closed.** |

| **B12** Parent Requests (`cf29827` → `48f1675`) | Migration `1777800001000-B12ParentRequests.ts`: 2 ENUMs (`parent_request_type`, `parent_request_status`) + 2 tables (`parent_requests`, `parent_request_messages`) + FORCE RLS + tenant_isolation policy + 4 indexes + CHECK constraint `((author_user_id IS NOT NULL)::int + (author_staff_id IS NOT NULL)::int) = 1` (XOR) + ON DELETE CASCADE on `parent_request_messages.parent_request_id` + REVOKE TRUNCATE from `shyraq_app`. ParentRequestModule: `ParentRequest` POJO entity (state machine `pending → {accepted, rejected, cancelled}` via `accept`/`reject`/`cancel` methods + `isPending`/`isReviewed` guards) + `ParentRequestMessage` POJO (XOR invariant in constructor) + 5 module errors (`ParentRequestNotFoundError` → 404, `ParentRequestStatusInvalidError` → 409, `ParentRequestAlreadyProcessedError` → 409, `ParentRequestForbiddenError` → 403 generic, `CreateRequestPermissionRequiredError` → 403). All transitions use **conditional UPDATE WHERE status='pending' RETURNING *** (db8cb72 race pattern) — service maps 0 rows to `ParentRequestAlreadyProcessedError`. Service ~1244 lines: `createTrustedPerson` (atomic OTP-validate + parent_request row), `createDayOff`/`createVacation`/`createLatePickup`/`createOpen` per-type creation; `accept(staff, requestId, reviewNote?)` with side-effects: `trusted_person` → creates `trusted_people` row + optional `pickup_requests` row (FK `parent_requests.id` → B11 column, atomic in ambient TX); `day_off`, `vacation`, `open_request` → notify only; `late_pickup` → `// TODO(B13): emit invoice` marker + notify. `reject` and `cancel` → status flip + notify. Thread CRUD: `addParentMessage`/`addStaffMessage` enforce author XOR at service entry + DB CHECK + entity invariant (3-layer defense). 3 controllers: `parent-parent-request.controller.ts` (parent under `ChildAccessGuard` + `@Roles('parent')`; OTP request + 5 create endpoints + cancel + thread), `staff-parent-request.controller.ts` (`KindergartenScopeGuard` + `@Roles('mentor','specialist','admin')`; staff inbox by recipient_staff_id / recipient_type='admin' / mentor's group; accept/reject/thread), `admin-parent-request.controller.ts` (`@Roles('admin')`; lists all in kg + accept/reject/thread). 13 DTOs all snake_case wire. OTP integration: shared `OtpStorePort` (auth) reused for trusted_person flow with namespace `otp:request:trusted-person:{userId}` TTL 300s + per-phone rate-limit `rate:otp:{phone}` shared with auth (5/hour). Per-user create rate-limit `rate:parent_requests:create:{userId}` (30/hour). Cross-module: `TrustedPersonRepository.create` called directly from `accept(trusted_person)` (no permission re-check); `PickupRequestRepository.createByParent` extended with optional `parentRequestId`; `ChildGuardianRepository.findApprovedActiveByUserAndChild` for `create_requests` permission gate (toggleable per docs — primary=true, secondary=true, nanny=false default; nanny override permitted per `endpoints.md` §4.13 + BP §11 explicit locked-list); `NotificationDispatcher` +4 event keys (`request.accepted`/`rejected`/`cancelled`/`message_sent`) with nanny-policy filter via `link.toState().role === 'nanny'` routing into `nannyUserIds` set. SMS template `parentRequestOtpTemplate(code, requestType)` added. **shared-kernel:** `day-of-week.vo.ts` moved from schedule module (was `src/modules/schedule/domain/value-objects/`); new `isWeekendDay(date, tz?)` helper; **all date validators (day_off weekend dates + vacation date_from/to + isWeekendDay) default to `Asia/Almaty` timezone via `Intl.DateTimeFormat`-backed helpers in shared-kernel** (T8 codex finding). day_off: `details.weekend_dates: ['YYYY-MM-DD', ...]` 1-2 dates each Sat/Sun in Asia/Almaty; vacation: `date_from..date_to` inclusive range. `late_pickup`: single date + `details.expected_time` HH:MM + tariff_amount_kzt placeholder. `open_request`: subject + message + attachments to admin/mentor/specialist. T5 opus review: 0 CRITICAL / 1 HIGH (nanny requester slipped through dispatcher when `create_requests=true` override) / 2 MEDIUM (silent NULL wipe of audit fields on partial patch; ISO_WEEKDAY_TO_DAY index 0 bogus 'sun') / 2 LOW / 13 OBS. T6 fix-pass `676da78` closed H1+M1+M2. T7 codex second-opinion: 0 CRITICAL / 1 HIGH (nanny override authorization gate — investigated as TOGGLEABLE per docs, no code change beyond comment + pinning test) / 2 MEDIUM (UTC date validators ignored Asia/Almaty contract; `group_id` filter advertised but ignored). T8 fix-pass `5545151` closed all 3 — Asia/Almaty TZ helpers in shared-kernel + `groupId` plumbed through repo INNER JOIN children on `current_group_id`. T9 e2e fix-pass `48f1675` closed 6 e2e failures: 2 prod bugs (TypeORM `date` column returned PG string, mapper now coerces to `Date`; `InvariantViolationError` now propagates caller-supplied code slug instead of generic) + 4 test bugs (specialist_type missing in fixture; OTP test full_name length; 422 vs 400 status code mismatches from class-validator vs DomainErrorFilter). Tests: unit 888 → **980** (+92); integration 1053 (+1 race spec `parent-request.race.integration.spec.ts`); e2e 155 → **325** (+15 B12 scenarios A–J + 3 day_off validators + admin list + cross-tenant phantom + group_id filter + open_request thread + late_pickup invoice-null hook + nanny override 403 + cancel pending 200 / cancel accepted 409 + concurrent accept race 409). Commit set T0 → T8 + T9: `cf29827 docs(b12) t0 doc-sync` → `e44cfc8 feat(b12) t1 migration` → `6e8decb feat(b12) t2 domain skeleton + day-of-week VO → shared-kernel` → `21f3183 feat(b12) t3 service+3 controllers+DTOs+OTP+cross-module` → `f3f58d8 test(b12) t4 e2e Scenarios A-J + race-integration` → `676da78 fix(b12) t6 review-pass H1/M1/M2` → `5545151 fix(b12) t8 codex review-pass H1/M1/M2` → `48f1675 fix(b12) t9 e2e fix-pass`. **B12 demo-ready — BP §6 closed. Deferred:** `late_pickup` invoice → B13; AttendanceService hooks for `accept(day_off)` + `accept(vacation)` (mark child_daily_status='on_vacation' / include weekend_dates in expected attendance) → B22; cursor pagination on parent-request list endpoints (T5 OBS) → B22. |

**Отличия v2 от старого репо:** TypeORM (не Prisma), `<x>.service.ts` per module (не CQRS use-case-классы), brocoders module layout с `domain/` + `dto/` + `infrastructure/persistence/relational/{entities,mappers,repositories}/` (не 4-слойный hexagonal `domain/application/infrastructure/interface`), RLS + явная передача `kindergarten_id` (не `prisma.$extends` ALS-only), FORCE ROW LEVEL SECURITY + два DB-role'а (`shyraq` superuser для миграций / `shyraq_app` non-owner для runtime).

**B5+ продолжается с этого baseline'а.**

---

## 1. Сквозные принципы (применяются в каждом батче)

1. **Docs-first.** Если батч добавляет endpoint/таблицу/event/queue — сначала обновляем соответствующий SoT-doc (по таблице в [`CLAUDE.md §3`](CLAUDE.md)), показываем diff, и только потом пишем код.
2. **Swagger-first.** Каждый endpoint получает `@ApiTags`, `@ApiOperation`, DTO с `@ApiProperty` + realistic `example`. Если в Swagger не виден — не считается сделанным.
3. **Multi-tenancy с первой строчки.** Каждый запрос идёт через `KindergartenScopeGuard` + `TenantContextInterceptor` (RLS GUC). Service.ts явно передаёт `kindergarten_id` в repo-вызовы. Cross-tenant phantom-row integration spec — обязательная часть каждой новой tenant-scoped таблицы.
4. **Тесты — часть батча, 4 уровня.** domain-unit (POJO/VO) + service-unit (in-memory fakes) + integration-spec (real PG/Redis, gated `INTEGRATION_DB=1`) + e2e (HTTP через Supertest).
5. **Никаких спекулятивных абстракций.** Базовые классы / generic helpers только когда есть 3+ повторений.
6. **External integrations через port/adapter — обязательно provider-agnostic.** Любой внешний сервис (SMS, push, payment, OFD, file storage, face, media-server, …) попадает в код через `abstract class <Service>Port` + `Mock<Service>Adapter`. Бизнес-логика (service.ts) **никогда** не импортирует конкретного вендора (Mobizon, Twilio, Halyk, FreedomPay, Firebase, S3, …). Switch адаптера — через env (`PUSH_PROVIDER=mock|fcm`, `SMS_PROVIDER=mock|mobizon|twilio`, `PAYMENT_PROVIDER=mock|halyk|freedompay|tiptoppay`, …) с default `mock`. Подключение реального вендора = новый адаптер-файл (1 файл, ~20-100 строк) + 1 строка в module + env-var. Бизнес-код, контроллеры, тесты — не трогаем. Подробнее см. **§1.1 Provider-agnostic policy** ниже.
7. **Прогресс — в этом файле.** Каждый sub-task в чек-листе; отметка `[x]` ставится сразу при завершении.

---

## 1.1 Provider-agnostic policy (как реализуется принцип №6)

**Цель:** функционал должен быть закрыт **до** того как выбран конкретный вендор любого внешнего сервиса. Когда вендор выбран — подключение делается без редактирования бизнес-логики, контроллеров, тестов и DTO. Это даёт три преимущества: (а) можно стартовать функциональные батчи прямо сейчас без блокировки на вендор-выбор; (б) можно менять вендора без переписывания кода (Halyk → FreedomPay, Mobizon → Twilio); (в) тесты гонятся против Mock-адаптеров без реальных credentials.

### Pattern (template для каждого нового external service)

1. **Port** — `abstract class <Service>Port` в подходящем модуле (`src/modules/<x>/` либо `src/shared-kernel/` если кросс-модульный):
   ```ts
   // src/modules/billing/payment-provider.port.ts
   export interface CreatePaymentInput { invoiceId: string; amountKzt: number; returnUrl: string; }
   export interface CreatePaymentResult { providerPaymentId: string; redirectUrl: string; }
   export abstract class PaymentProviderPort {
     abstract createPayment(input: CreatePaymentInput): Promise<CreatePaymentResult>;
     abstract verifyWebhook(headers: Record<string, string>, body: unknown): Promise<{invoiceId: string; status: 'paid'|'failed'} | null>;
     abstract refund(providerPaymentId: string, amountKzt: number): Promise<void>;
   }
   ```
2. **MockAdapter** — обязательно. Возвращает детерминированные значения, логирует в `Logger`, никаких внешних HTTP-вызовов. Это default для CI / dev / demo:
   ```ts
   @Injectable() class MockPaymentProvider extends PaymentProviderPort { /* ... */ }
   ```
3. **RealAdapter — опционально на момент батча.** Если есть TODO-stub (как у `FcmPushAdapter` сегодня) — он должен **throw** с явным сообщением `<Provider> adapter not implemented; configure <ENV>=mock`. Никаких silent-no-op.
4. **Env-switch** в module:
   ```ts
   {
     provide: PaymentProviderPort,
     useFactory: () => {
       const p = (process.env.PAYMENT_PROVIDER ?? 'mock').toLowerCase();
       if (p === 'halyk') return new HalykPaymentAdapter(...);
       if (p === 'freedompay') return new FreedomPayPaymentAdapter(...);
       return new MockPaymentProvider();
     },
   }
   ```
5. **Бизнес-код инжектит порт.** Никаких `if (provider === 'halyk')` в `service.ts`. Никаких импортов из `node_modules` вендора в `service.ts`/`controller.ts`/`dto/`. Адаптер — единственное место, где живёт вендор-specific код.
6. **Тесты:** unit использует in-memory fake адаптера (рукописный, не Jest auto-mock); integration/e2e — `MockAdapter`.
7. **Конфиг.** Каждый порт сопровождается `<service>.config.ts` через `registerAs(...)` (как `redis.config.ts`/`auth.config.ts`) если нужны вендор-credentials. Mock не требует конфига.
8. **Документация.** В `env-example-relational` добавляется `<SERVICE>_PROVIDER=mock` (default) + закомментированные строки для каждого реального вендора с описанием `# <Provider>: requires <ENV_VAR_LIST>`.

### Чеклист для review каждого батча, который трогает external service

- [ ] Порт абстрактный? (`abstract class`, не interface — DI-токен совпадает с классом)
- [ ] MockAdapter существует и default?
- [ ] Env-switch реализован через `useFactory`?
- [ ] Бизнес-код **не импортирует** вендор-specific пакеты?
- [ ] `env-example-relational` содержит `<SERVICE>_PROVIDER=mock` с комментариями?
- [ ] Real-adapter (если есть stub) — throws явной ошибкой при вызове?
- [ ] Unit-тесты используют in-memory fake; e2e — Mock?
- [ ] Документ `endpoints.md`/`architecture.md` ссылается на абстракт-порт, не на конкретный вендор?

### Текущий статус ports & adapters (что уже provider-agnostic)

| Сервис | Port | Mock | Real | Env-switch |
|---|---|---|---|---|
| SMS | `SmsPort` | `MockSmsAdapter` ✓ | — | ❌ (B22 добавит) |
| Push | `PushNotificationPort` | `MockPushAdapter` ✓ | `FcmPushAdapter` (stub throws) | ✓ `PUSH_PROVIDER` |
| JWT signing | `JwtTokenPort` | `JsonwebtokenJwtAdapter` ✓ | — | n/a (jsonwebtoken — не вендор-lock) |
| Password hash | `PasswordHasherPort` | `BcryptPasswordHasherAdapter` ✓ | — | n/a |
| OTP store | `OtpStorePort` | `RedisOtpStoreAdapter` ✓ | — | n/a (Redis — инфра, не вендор) |
| Token blocklist | `TokenBlocklistPort` | `RedisTokenBlocklistAdapter` ✓ | — | n/a |
| Pickup OTP store | `PickupOtpStorePort` | `RedisPickupOtpStoreAdapter` ✓ | — | n/a |
| Parent-request OTP store | `ParentRequestOtpStorePort` | Redis adapter ✓ | — | n/a |
| QR token cache | `QrTokenCachePort` | `RedisQrTokenCacheAdapter` ✓ | — | n/a |
| QR scan rate-limit | `QrScanRateLimiterPort` | `RedisQrScanRateLimiterAdapter` ✓ | — | n/a |
| Notification dispatch | `NotificationPort` | `OutboxNotificationAdapter` ✓ + `LoggingNotificationAdapter` | — | n/a |
| WS broadcaster | `WsBroadcasterPort` | socket.io adapter ✓ | — | n/a |
| Clock | `ClockPort` | `SystemClockAdapter` + `FixedClockAdapter` (тесты) | — | n/a |

### Что появится в следующих батчах (порт + Mock — обязательно в момент батча)

| Batch | Сервис | Port | Mock | Real adapter |
|---|---|---|---|---|
| **B13** | Payment provider | `PaymentProviderPort` | `MockPaymentProvider` | — (B14 добавит Halyk; FreedomPay/TipTopPay позже параллельно) |
| **B15** | OFD/фискальный чек | `FiscalReceiptPort` | `MockFiscalReceiptAdapter` | — (выбор Kassa24/Rekassa/Webkassa отложен) |
| **B17** | File storage (фото/видео для контента) | `FileStoragePort` | `LocalFileStorageAdapter` (диск) | — (S3/Yandex Object Storage адаптер позже) |
| **B17** | Push tags (если для broadcast topics) | расширение `PushNotificationPort` | — | — |
| **B19** | Face embeddings | `FaceEmbeddingPort` | `MockFaceEmbeddingAdapter` | edge-attached real adapter |
| **B19** | Vector DB (Qdrant) | `FaceVectorDbPort` | `InMemoryFaceVectorDbAdapter` | Qdrant adapter |
| **B20** | Media server (CCTV) | `MediaServerPort` | `MockMediaServerAdapter` | MediaMTX adapter |

**Обязательное требование к каждому функциональному батчу:** если он касается внешнего сервиса — **порт + Mock закрываются в самом батче**. Real-adapter может ждать — функционал не ждёт.

### Что отложено явно до закрытия всего функционала

См. **§2.1 Phase order** ниже. До закрытия B22 **никаких real-vendor-адаптеров не пишем** (кроме случаев, когда работает Mock + Real уже подключён старым кодом — например, FCM stub останется как есть). Real-vendor PR'ы делаем после prod-ready функционала отдельным "Integrations Phase".

---

## 2.1 Phase order — функционал → интеграции → edge/face/cctv

Решение **2026-05-07:** идём в три фазы. Каждая следующая стартует только после полного закрытия предыдущей.

### Phase A — Functional completeness (текущая)

Цель: закрыть **весь функционал** против Mock-адаптеров. Никаких real-vendor подключений в этой фазе. Phase A считается закрытой когда вся бизнес-логика BP §1–§13 (исключая Face/CCTV) работает end-to-end на Mock-адаптерах + B22 polish сделан.

**Порядок батчей:**

| # | Батч | Зависимости | Что закрывает |
|---|---|---|---|
| 1 | **B13** Billing & Invoices (mock) | — | BP §4 функционал |
| 2 | **B16** Custom Discounts | B13 | BP §4.1 |
| 3 | **B17** Content & Stories | — (⊥ B18) | BP §9 |
| 4 | **B18** Diagnostics & Progress | — (⊥ B17) | BP §8 |
| 5 | **B21** Lifecycle | B13 | BP §12 |
| 6 | **B22** Polish | всё выше | i18n, security, Swagger, технический долг §5 |

После B22 → **Phase A done = функционально prod-ready**. Можно демонстрировать клиенту, можно начинать пилот на одном саду с Mock-SMS / Mock-Payment (только для теста UX, не для денег).

### Phase B — Real integrations

Цель: подключить выбранных вендоров. Каждый PR здесь = **1 файл-адаптер + env-switch строка + entry в `env-example-relational`**. Бизнес-код не трогается (если потребовалось трогать — порт спроектирован неправильно, возвращаемся в Phase A для рефакторинга порта).

| # | Батч | Что подключает | Блокер |
|---|---|---|---|
| — | **Real SMS adapter** | Mobizon / SMSC / Twilio / другой | выбор провайдера |
| — | **B14** Halyk ePay (real money) | Halyk webhook + redirect flow | Halyk ePay контракт |
| — | **B15** OFD (Fiscal) | Kassa24 / Rekassa / Webkassa | выбор ОФД |
| — | **Дополнительные платёжки** | FreedomPay / TipTopPay (опционально, параллельно Halyk) | выбор |
| — | **Real File Storage** | S3 / Yandex Object Storage | выбор + creds |
| — | **Real FCM** | firebase-admin в `FcmPushAdapter` (сейчас throws) | Firebase project |

После Phase B → **полностью prod-ready, можно принимать деньги, отправлять SMS, печатать чеки**.

### Phase C — Edge / Face / CCTV

Cloud-only функционал прода живой. Edge-стек разворачивается per kindergarten.

| # | Батч | Зависимости |
|---|---|---|
| — | **B18.5** Edge Bootstrap | Phase A done (cloud стабилен) |
| — | **B19** Face ID | B18.5 |
| — | **B20** CCTV | B18.5 |

---

## 2. Карта батчей (high-level)

| # | Батч | BP / тех-блок | Цель demo |
|---|---|---|---|
| ~~B0~~ | ~~Foundation~~ | — | replaced by P0–P1 |
| ~~B1~~ | ~~Auth & Identity~~ | — | replaced by P2 |
| ~~B1.5~~ | ~~Hexagonal Refactor~~ | — | n/a (v2 starts brocoders, not hexagonal) |
| ~~B2~~ | ~~Tenant Bootstrap~~ | — | replaced by P3 |
| ~~B3~~ | ~~Organization~~ | — | replaced by P4 |
| ~~B4~~ | ~~Children & Guardians~~ | — | replaced by P5 |
| **B5** | **Enrollment** | BP §1 целиком | Полный путь lead → in_processing → card_created → активный ребёнок. **Demo-ready!** |
| **B6** | **Parent Onboarding** | BP §2 целиком | Родитель заходит в приложение по OTP, привязывает ребёнка по ИИН, primary одобряет. **Demo-ready!** |
| **B7** | **Schedule & Meal** | BP §9 (часть) + ScheduleModule | Templates + slots + activity_events; meal_plans с auto-copy cron |
| **B8** | **Attendance & Timeline (manual)** | BP §5 без Face/QR | Manual check-in/out → timeline → daily_status → push родителю. **Demo-ready (BP §5 manual)!** |
| **B9** | **Notifications + WebSocket** | Shared §0.4 + WS gateway | Push + WS rooms, preferences (mute), notification history. Process split: api + worker + ws. |
| **B10** | **Identity QR** | BP §13 целиком | Refreshable QR в Parent/Staff app, scan endpoint, fallback при Face fail. **Demo-ready!** |
| **B11** | **Pickup OTP & Trusted** | BP §7 целиком | Whitelist, OTP по SMS, валидация, фиксация в attendance. **Demo-ready!** |
| **B12** | **Parent Requests** | BP §6 целиком | 5 типов заявок (`trusted_person`, `day_off`, `vacation`, `late_pickup`, `open_request`), статус-машина `pending → {accepted, rejected, cancelled}` через conditional UPDATE, двусторонний тред сообщений (`parent_request_messages` + author XOR). OTP-flow для `trusted_person` (1-стадийно: `/parent/requests/otp-request` → `/parent/requests/trusted-person`). `day_off` = ребёнок В САДУ в выходной (1-2 даты, каждая Сб/Вс); `vacation` = ребёнок НЕ ХОДИТ (date_from..date_to). Asia/Almaty TZ-aware валидаторы (shared-kernel helpers). day-of-week VO перенесена из schedule в shared-kernel. invoice_id nullable (B13 hook). Tests: unit 980 / integration 1053 (1 known B22 flake) / e2e 325 — все зелёные. **Demo-ready!** (commits `cf29827` → `48f1675`) |
| **B13** | **Billing & Invoices (mock provider)** | BP §4 (бэк-часть) | Тарифы, assignments, cron генерации invoice 1-го числа, pro-rata, holidays, `PaymentProviderPort` + `MockPaymentProvider`. **Phase A** |
| **B16** | **Custom Discounts** | BP §4.1 | Conditions engine, batch-notify, auto-apply на invoice. **Phase A** |
| **B17** | **Content & Stories** | BP §9 целиком | News, Qundylyq, birthday auto-gen, group stories 24h TTL, auto-copy menu/schedule. `FileStoragePort` + `LocalFileStorageAdapter`. **Phase A** |
| **B18** | **Diagnostics & Progress** | BP §8 целиком | Templates → entries → progress notes → видимость родителю. **Phase A** |
| **B21** | **Lifecycle** | BP §12 целиком | Архивация, переводы между группами, смена ментора, пересчёт оплат. **Phase A** |
| **B22** | **Polish** | i18n, validators, security headers, Swagger финал, технический долг §5 | Production-ready hardening. **Phase A — закрывает функционал** |
| ⏸ **B14** | **Payment Provider — Halyk** *(Phase B — отложено до закрытия B22)* | BP §4 (real money) | Real `HalykPaymentAdapter` файл подключается к существующему `PaymentProviderPort`. Бизнес-код B13 не трогается. |
| ⏸ **B15** | **OFD (Fiscal)** *(Phase B — отложено до закрытия B22)* | BP §4 add-on | Real `<Vendor>FiscalReceiptAdapter` к существующему `FiscalReceiptPort` (порт заводится в B15 Phase-A skeleton либо в момент real-подключения). |
| ⏸ **Real SMS** | **SMS-провайдер adapter** *(Phase B — отложено до закрытия B22)* | — | Real `<Vendor>SmsAdapter` к существующему `SmsPort`. Env-switch `SMS_PROVIDER=mock|<vendor>` добавляется в этом PR. |
| ⏸ **Real Push** | **Firebase Admin** *(Phase B — отложено до закрытия B22)* | — | Заполнить body `FcmPushAdapter` (сейчас throws). |
| ⏸ **Real File Storage** | **S3 / Yandex Object Storage** *(Phase B — отложено до закрытия B22)* | — | `S3FileStorageAdapter` к `FileStoragePort` из B17. |
| ⏸ **B18.5** | **Edge Bootstrap** *(Phase C — отложено до закрытия Phase B)* | архитектура (D15) — cloud↔edge protocol | edge-agent skeleton, pairing-flow, mTLS, `edge_commands` / `edge_health` / `kindergarten_edge_credentials` tables. Блокирует B19/B20. |
| ⏸ **B19** | **Face ID** *(Phase C — отложено)* | BP §5 (Face-часть) | Consent → enrollment → identification → check-in. Edge-attached. |
| ⏸ **B20** | **CCTV** *(Phase C — отложено)* | BP §11 целиком | MediaMTX, Nginx auth_request, локальные edge-tokens, WS-trigger при смене локации. |

**Параллелизация в Phase A:** B17 ⊥ B18 — могут идти параллельно если есть руки. B13 → (B16, B21) → B22.

**Параллелизация в Phase C:** B19 ⊥ B20 только после закрытия B18.5 — оба батча зависят от уже работающего cloud↔edge канала и pairing'а.

**⏸ — отложенный батч:** старт по сигналу пользователя после закрытия предыдущей фазы (или явного override). Каждый ⏸-батч уже спроектирован так, что его подключение не требует изменений в бизнес-логике (см. §1.1 Provider-agnostic policy).

---

## 3. Шаблон батча (B5+)

Перед стартом каждого батча:

1. **Doc-sync.** Какие из 4 SoT-doc'ов трогаем? Покажи diff пользователю, получи "ок".
2. **Migration.** Если новые таблицы/колонки — `npm run migration:create -- --name=<batch>`. RLS policy + `FORCE ROW LEVEL SECURITY` + indexes (включая partial-uniques) в той же миграции.
3. **Module skeleton.** Создать NestJS модуль по template из [`CLAUDE.md §4`](CLAUDE.md): `domain/` (entity/VO/error), `dto/`, `infrastructure/persistence/relational/{entities,mappers,repositories}/`, `<x>.repository.ts` (abstract), `<x>.service.ts`, `<x>.controller.ts`, `<x>.module.ts`. Зарегистрировать в `AppModule`.
4. **Endpoints с Swagger-аннотациями.** Каждый endpoint виден в `/docs`. DTO с `@ApiProperty({example})` + `@ApiResponse` 200/201/400/401/403/404/409/422/429.
5. **Multi-tenancy enforcement.** Service.ts явно передаёт `kindergartenId` в repo. RLS policy на новой таблице (см. §2 architecture.md). Cross-tenant phantom-row integration spec.
6. **Tests по 4 уровням.** domain-unit + service-unit (in-memory fakes) + integration-spec (real PG/Redis) + e2e (HTTP).
7. **Manual smoke.** Прогон через Swagger UI: ключевые BP-сценарии отрабатывают.
8. **Update progress.** Поставить `[x]`; если демо-ready — отметить в Demo-Ready секции.

---

## 4. Прогресс-трекер

> Маркер: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

### Foundation (replaced by P0–P5 in v2)

- [x] **P0** Bootstrap (commit `4998f81`)
- [x] **P1** Foundation (commit `018b07d`)
- [x] **P2** Auth (commits `cacb396` → `b9ea1f9` → `f8ad876` → `5b727b1` → `c87b689`)
- [x] **P3** Tenant Bootstrap (commit `0279633`)
- [x] **P4** Organization (commit `319e82f`)
- [x] **P5** Children & Guardians (commit `f1d6984`) — **B4-parity reached**

### BP-батчи (v2 продолжает с B5)

- [x] **B5** — Enrollment ✓ **demo-ready: BP §1**
- [x] **B6** — Parent Onboarding ✓ **demo-ready: BP §2** (commit `c4bc35b`)
- [x] **B7** — Schedule & Meal ✓ (commit `b27b5dc`)
- [x] **B8** — Attendance & Timeline (manual) ✓ **demo-ready: BP §5 manual** (commit `05cddc2`)
- [x] **B9** — Notifications + WebSocket ✓ (commit `e704acd`)
- [x] **B10** — Identity QR ✓ **demo-ready: BP §13** (commits `75c27b0` → `18281df`)
- [x] **B11** — Pickup OTP & Trusted ✓ **demo-ready: BP §7** (commits `795bf9c` ← `61a6853` ← `b945304` ← `de67024` ← `031373c` ← `2365f4f` ← `2833d72` ← `5e47ed9`)
- [x] **B12** — Parent Requests ✓ **demo-ready: BP §6** (commits `cf29827` → `48f1675`)

**Phase A — Functional completeness (текущая фаза, идём по порядку)**

- [x] **B13** — Billing & Invoices (mock provider) ✓ **demo-ready: BP §4** (commits `da84db6` → `df1c9af`)
- [x] **B16** — Custom Discounts ✓ **demo-ready: BP §4.1** (commits `53f17c4` → `8c2b094`)
- [x] **B17** — Content & Stories ✓ **demo-ready: BP §9** (commits `8abeed2` → `14fa714`)
- [x] **B18** — Diagnostics & Progress ✓ **demo-ready: BP §8** (commits `8abeed2` (joint T0) → `baf608b` → `3554b01` → `a89e884` → `1796781` → `7e11ab0` → `f478470`)
- [ ] **B21** — Lifecycle *(требует B13)*
- [ ] **B22** — Polish — i18n, security headers, Swagger финал, технический долг §5

**Phase B — Real integrations (отложено до закрытия B22)**

- [⏸] **Real SMS adapter** — выбор провайдера + `<Vendor>SmsAdapter` + env-switch
- [⏸] **B14** — Halyk ePay (real) — `HalykPaymentAdapter` к `PaymentProviderPort` из B13
- [⏸] **B15** — OFD — `<Vendor>FiscalReceiptAdapter`
- [⏸] **Real FCM** — заполнить `FcmPushAdapter` body (сейчас throws)
- [⏸] **Real File Storage** — `S3FileStorageAdapter` к `FileStoragePort` из B17
- [⏸] Дополнительные платёжки (FreedomPay / TipTopPay) — параллельно Halyk если потребуется

**Phase C — Edge / Face / CCTV (отложено до закрытия Phase B)**

- [⏸] **B18.5** — Edge Bootstrap *(cloud↔edge protocol, pairing, mTLS — блокирует B19/B20)*
- [⏸] **B19** — Face ID
- [⏸] **B20** — CCTV

### Demo-able BP

- [x] **BP §1** Enrollment & Onboarding *(часть — бутстрап tenant'а + первого admin'а закрыта в P3; enrollment детей — закрыта в B5: leads, state machine transition→card_created)*
- [x] **BP §2** Parent App Onboarding *(B6: link/unlink по ИИН + auto-approve primary на OTP verify — demo-ready)*
- [x] **BP §3** Staff & Admin Provisioning *(P3 + P4: первый admin — атомарный POST /super-admin/kindergartens; остальные staff — POST /admin/staff с role×specialist_type matrix, TX-атомарность create+mentor-assign, deactivate/activate symmetric, mentor assign/change-primary с partial-idx invariants)*
- [x] **BP §4** Payments *(B13: tariffs + invoices + payments + refunds + holidays + monthly cron + Mock adapters + provider-agnostic ports — demo-ready on Mock provider; B14/B15 will plug real Halyk/OFD)*
  - [x] **BP §4.1** Custom Discounts *(B16: conditions engine with 10 condition types + all_of/any_of composition, 5 targeting modes, stacking algorithm NS-gates-further, advisory-lock race protection, DiscountExpireProcessor daily cron, 9 admin endpoints, InvoiceService integration — demo-ready on Mock provider; `53f17c4` → `8c2b094`)*
- [x] **BP §5** Daily Operations & Attendance *(B8 manual: check-in/check-out, timeline CRUD, daily status — Face/QR/OTP-pickup deferred to B19/B10/B11)*
- [x] **BP §6** Parent Requests *(B12: 5 типов заявок, OTP-flow для trusted_person, state-machine pending → {accepted, rejected, cancelled} через conditional UPDATE, двусторонний тред, Asia/Almaty TZ-aware валидаторы, day-of-week VO в shared-kernel, late_pickup invoice hook B13-ready)*
- [x] **BP §7** Pickup & Trusted Person OTP *(B11: staff/parent pickup-request flow with OTP, trusted_people whitelist with is_one_time auto-deactivate, advisory-lock-protected validate, attendance integration via method='otp_pickup', notifications pickup.otp_sent + pickup.validated)*
- [x] **BP §8** Diagnostics & Progress *(B18: templates → entries (specialist) → progress notes (mentor) → my-todos (6-month staleness Asia/Almaty) → parent visibility w/ view_diagnostics permission gate (nanny excluded). 3 tables + FORCE RLS + cross-tenant phantom spec. 19 endpoints across 6 controllers. Outbox events `diagnostic.new` + `progress_note.new` to approved-active guardians.)*
- [x] **BP §9** Content Management *(B17: content_posts state machine + group_stories 24h TTL + FileStoragePort + 4 services + 3 BullMQ processors + 16 endpoints + parent feed aggregation + birthday auto-gen with advisory lock + nanny-policy excluded — demo-ready on Mock LocalFileStorageAdapter; B22+ adds real S3/Yandex)*
- [x] **BP §10** Notifications & Parent Visibility *(B9: Outbox + WS gateway + push fanout — partial; per-actor matrix continues across batches)*
- [ ] **BP §11** CCTV
- [ ] **BP §12** Lifecycle Management
- [x] **BP §13** Identity QR System *(B10: GET /users/me/qr, POST /staff/qr/scan, POST /admin/qr/revoke-all/:userId — cross-tenant token, kg-scoped linkedChildren, advisory-lock-protected issue, rate-limit 60/min/device; T7-2: reuse-flow restored via `qr:user:{userId}:identity` paired Redis key, admin revoke-all kg-scoped + 404 on missing user)*

---

## 5. Открытые вопросы

### Active

- **SMS-провайдер** (Kazakh local vs Twilio fallback) — блокирует production-launch. На время отсутствия real-provider mock допускается даже в `NODE_ENV=production`; start-up guard добавится PR'ом подключения real-provider.
- **OFD-провайдер** (Kassa24 / Rekassa / Webkassa) — блокирует запуск B15 в demo.
- **TipTopPay vs FreedomPay** — выбрать один. Блокирует расширение B14.
- **Qdrant vs pgvector** для face embeddings — блокирует начало B19. (D15 фиксирует Qdrant локально на edge; pgvector вариант снят с рассмотрения.)
- **IIN Luhn checksum validation** — валидируется только формат `^\d{12}$`. Контрольная цифра по алгоритму ИИН РК (Luhn-mod-вариант) откладывается до v2-polish.
- **child.service.ts split (B6 leftover)** — после B6 файл вырос до ~880 строк (было ~700). CLAUDE.md §8 рекомендует разбить на role-specific (`child.service.ts` admin + `parent-child.service.ts` parent-flow). Не сделано в B6, чтобы не пересекаться с B5/P5 рефакторингом в одном PR. Маркер `// TODO(refactor): split child.service.ts on parent vs admin paths` стоит в `src/modules/child/child.service.ts` рядом с `linkChildByIin`. Отдельная задача после B7 или одной volna с B12 (когда parent-flow ещё расширится).
- **enrollment integration ordering flake (B5 leftover)** — `enrollment_status_log` ORDER BY `created_at ASC` fails when two rows share identical timestamps. Pre-existing; surfaced clean by B7 verification. Tolerated (not blocking B7). Fix in B22 polish or whoever next touches enrollment.
- **Commit `9c16743` mis-titled** — sub-agent message-race during parallel T3/T4 run committed ScheduleModule contents under "meal module" message. Body of follow-up commit `18bdc8c` documents the slip. Not amended (CLAUDE.md §10 forbids amend without explicit request). Re-amend if/when user OKs.
- **`mentor-group scope on timeline writes` (B8 T4 observation)** — staff timeline POST/PATCH/DELETE currently allows any active staff in kg, not just the child's group mentor. Tightening this requires resolving "current group" + mentor assignment for each request — defer to B22 polish or a dedicated security pass when staff role boundaries are formally specified. Marker `// TODO(B22): tighten mentor-group scope on timeline writes` in `src/modules/attendance/staff-timeline.controller.ts`.
- **Outbox payload schema drift (B9 T10 observation)** — `notification_outbox.payload` is `Record<string, unknown>`; `NotificationDispatcher` reads named fields at runtime without compile-time guarantees. Future: typed payload contracts in shared-kernel (B22 polish). No `// TODO` marker needed.
- **Notification locale resolution (B9 T10 observation)** — dispatcher uses `titleI18n.ru` for FCM title; client picks locale from raw jsonb in REST. Future: per-user locale lookup via `UsersRepository` in dispatcher (B22 polish).
- **failed-event prune job (B9 observation)** — `notification_outbox` rows with `status='failed'` accumulate forever (partial idx excludes them from poll, no perf hit, just table growth). Future: monthly prune job (B22 polish).
- **OutboxPollerProcessor sequential per-event (B9 observation)** — for high-volume tenants in-batch dispatch latency stacks linearly. Future: `Promise.all` once per-event TX-isolation is verified at scale (B22 polish).
- **Advisory lock through `IdentityQrRepository.acquireUserAdvisoryLock` (B10 T7-2 follow-up)** — `IdentityQrRepository.acquireUserAdvisoryLock(userId)` uses `manager()` helper which falls back to `repo.manager` when no ambient TX is present. No ambient TX = fresh connection from pool; lock auto-releases at COMMIT on that connection. Lock semantics correct in all caller paths today. Re-verify if a long-running worker invokes `issueOrRefresh` outside any TX.
- **Refresh-token bypass_rls audit (B10 T7-2 follow-up)** — `RefreshTokenRelationalRepository.create` (no-tenant-context branch) and `rotate` still wrap in `manager.transaction(...)` + `SET LOCAL app.bypass_rls='true'`. Both run from places where the row's kg may not be set in GUC (OTP-verify happens before `KindergartenScopeGuard`), so bypass is genuinely needed. Outside HTTP-ambient TX, the wrapper opens a fresh pool connection so leak is bounded. No action needed but worth a comment audit at some point. Defer to B22 polish.
- **`UserNotFoundError` code override pattern (B10 T7-2 minor)** — `Object.defineProperty` on inherited `code` is non-standard. Long-term: `NotFoundError` could grow optional `codeOverride` constructor param for resource-specific subclasses. Defer to B22 polish.
- **Phone PII exposure on staff list (B11 T6 H4)** — `GET /staff/pickup-requests` returns full `trusted_person_phone` in plaintext to every mentor + admin in the kindergarten. Tenant-internal but harvestable across the whole staff app session. Future: mask in list response (`+7707***4567`), require a separate audited endpoint for the full number when explicitly fetched (`GET /staff/pickup-requests/:id`). Defer to B22 polish or compliance review when the data-handling story for phone numbers is formalised.
- **B13 T9/T10 review carry-forwards (MEDIUM/LOW)** — closed in §5 Active for B22 polish:
  - **payment-calendar UTC startOfMonth** (T10 NEW-03) — `src/modules/billing/invoice.service.ts:546` `buildPaymentCalendar` uses `startOfMonth(today)` in UTC; around Almaty local month rollover (e.g. `2026-05-31T20:30Z = 2026-06-01 01:30 Asia/Almaty`), parent calendar starts in May. Use shared-kernel `monthly-billing.processor.computePeriodStart` Asia/Almaty pattern. B22 polish.
  - **invoice.overdue event no caller** (T10 NEW-04) — `src/modules/billing/invoice.service.ts:863` template + adapter method exist, but no producer call site; overdue-marking nightly cron deferred. Closed when overdue-marking job lands (B22).
  - **MonthlyBillingProcessor partial-failure not retried** (T9 MEDIUM#8) — per-kg errors swallowed; the only path that retries is `listAllKindergartens` failing. Throw `MonthlyRunPartialFailureError` when `errors === kgIds.length` so attempts retry whole-batch fail. B22.
  - **Refund reject endpoint not wired** (T9 LOW#5) — `RefundService.reject` exists, controller doesn't expose it. 2 e2e scenarios (S) skipped. B22.
  - **TariffPlan overlap check not enforced** (T9 LOW#1) — `// TODO(B13 review): non-overlapping valid_from..valid_until per (kg,applies_to,group_id?,tariff_type)` in `tariff-plan.service.ts:42-46`. B22.
  - ~~**InvoiceService siblingsCount not wired to MockDiscountEngine** (T9 LOW#2)~~ — *closed in B16 T3 — see §5 Resolved.*
  - **`Refund.reject()` reuses `reason` column** (T9 LOW#6) — design intentional but `processed_by` may be NULL on rejected; minor type drift. B22.
  - **Migration `up()` not idempotent on rerun** (T10 NEW-05) — wrap `CREATE TYPE` в `DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;`. B22 polish (low risk; migrations don't rerun in normal flow).
  - **Currency hardcoded KZT** (T10 NEW-06) — `payment.service.ts:231` hardcodes `'KZT'` even though tariff_plans support a currency field. KZT-only сегодня; multi-currency = future feature. Document or enforce KZT-only at DTO. B22.
  - **WorkerModule doesn't import BillingModule** (T4b sub-agent flagged) — currently MonthlyBillingProcessor only registers in api process. If api+worker split happens, worker won't have the cron. Add `BillingModule` to `WorkerModule.imports` + set `BILLING_MONTHLY_CRON=disabled` on api in production. B22.
  - **Migration to Decimal-backed money type** (T11 H7 ambition level 1) — `// TODO(B22): migrate to Decimal-backed money type`. Current `roundKzt` minimum-fix pattern; full Decimal migration post-functional-completeness.

- **B16 T6/T7 review carry-forwards (MEDIUM/LOW)** — closed in §5 Active for B22 polish:
  - **audit-row mismatch on `incrementUsedCount` skip-with-warn** (T6 H2) — when the post-engine `incrementUsedCount` returns false (total_max_uses raced) we log+skip the application row, but the line-item carrying the discount amount is already in the persisted invoice. Audit ledger then under-counts vs the displayed discount. Plumb `metadata.skipped_reason` into the application table or compensate the line item. Path: `src/modules/billing/invoice.service.ts:1135-1146`. B22 polish.
  - **7 default-no-op repository methods** (T6 M1) — several `CustomDiscountRepository` and `CustomDiscountApplicationRepository` methods have `Promise.resolve()` defaults in fakes that hide missing implementations. Convert defaults → abstract; add explicit stubs in test fakes. Mechanical sweep. Path: `src/modules/billing/custom-discount.repository.ts`, `custom-discount-application.repository.ts`. B22.
  - **Migration `update_updated_at_column()` not dropped in down()** (T6 L1) — the trigger function is created in `up()` but `down()` only drops the tables; the function lingers. Trivial. Path: `src/database/migrations/1777890000000-B16CustomDiscounts.ts`. B22.
  - **`roundKzt` consistency in stacking subtraction** (T6 L2) — the engine uses raw arithmetic (`amountDue * (amount/100)` then `roundKzt`) but `applyStackingAndAmounts` accumulates without re-rounding between steps. Currently safe for single-pass but mixed percentage+fixed orderings could drift by 0.01. Path: `mock-discount-engine.adapter.ts:230-244`. B22.
  - **REVOKE TRUNCATE not restored in down()** (T6 L4) — the migration `REVOKE`s TRUNCATE on the new tables but the `down()` doesn't `GRANT` it back. Path: same migration. B22.
  - **Scenario M bypasses BullMQ** (T7 SO-2) — the daily-expire e2e exercises the SaaS HTTP trigger path which calls the processor inline; same trade-off as B13 monthly-run e2e. Defer to B22 BullMQ test runner sweep. Path: `test/custom-discounts.e2e-spec.ts:1102-1153`.
  - **Additional e2e coverage + scenario reorg** (T6 OBS-1..5) — e.g. consolidate the inline target_type smoke into the M2 list filter scenario; add OBS scenarios for tariff_types targeting; add cross-tenant negative for the apply-race lock. Defer to B22.

- **B18 T6/T7 review carry-forwards (MEDIUM/LOW)** — closed in §5 Active for B22 polish:
  - **T6-M4 race protection on PATCH** — `DiagnosticTemplateService.update()`, `DiagnosticEntryService.update()`, and `ProgressNoteService.update()` do unconditional read → modify → write without `findByIdForUpdate` + optimistic-lock version guard. Concurrent PATCHes on the same template/entry/note can silently lose an update. Deferred per M4 doc-comment. Path: `src/modules/diagnostics/diagnostic-template.service.ts:65-76`, `diagnostic-entry.service.ts`, `progress-note.service.ts`. B22.
  - **T6-M6 N+1 template lookup in `buildTemplateLookup`** — per-entry `DiagnosticTemplateService.getById()` calls in `StaffDiagnosticEntryController` and `ParentDiagnosticController` result in N round-trips for N unique template ids in a list response. Add `DiagnosticTemplateRepository.listByIds(kgId, ids)` (`WHERE id = ANY($1)`) + update `buildTemplateLookup`. Path: `src/modules/diagnostics/staff-diagnostic-entry.controller.ts:62-78`, `parent-diagnostic.controller.ts:53-71`. B22.
  - **T6-M7 Almaty TZ calendar-date formatting** — `assessmentDate.toISOString().slice(0,10)` in `DiagnosticEntryService` and the relational repository formats the date in UTC, not Almaty. Contract implicit today (controller parses `'YYYY-MM-DD'` to UTC midnight). Centralise via `date.toLocaleDateString('en-CA', { timeZone: 'Asia/Almaty' })` shared-kernel helper. Path: `src/modules/diagnostics/diagnostic-entry.service.ts:96`, `diagnostic-entry.relational-repository.ts:67`. B22.
  - **T6-L1 stale race-protection TODO comments** — `"race protection can be wired later"` doc-comments remain in `diagnostic-template.service.ts` and `progress-note.service.ts` after T8 deferred M4. Replace with ADR/plan link. B22.
  - **T6-L2 audit-log on orphaned template branch** — `DiagnosticEntryService` "should be unreachable" FK-RESTRICT branch throws generic 404 silently. Add `Logger.error('orphaned_diagnostic_entry id=', id)` before throw so orphaned entries surface in logs. Path: `src/modules/diagnostics/diagnostic-entry.service.ts:124-128`. B22.
  - **T6-L3 parent DTO drops unused fields** — `ParentDiagnosticController.listDiagnostics` accepts `ListDiagnosticEntriesQueryDto` (includes `specialist_id` + `template_id`) but does not forward them. Swagger advertises filters that do not filter. Create dedicated `ParentListDiagnosticEntriesQueryDto` or forward them in service. Path: `src/modules/diagnostics/dto/list-diagnostic-entries-query.dto.ts`. B22.
  - **T6-L5 diagnostics module exports** — `DiagnosticsModule` exports repository providers (`*Repository`), breaking encapsulation (other modules export only services). Drop `*Repository` from `exports`; keep services only. Path: `src/modules/diagnostics/diagnostics.module.ts:54-66`. B22.
  - **T7-MEDIUM Child module boundary leak** — `MyTodosService` and related diagnostics services import `ChildRepository` + `ChildGuardianRepository` directly from `child/infrastructure/persistence` (CLAUDE.md §4 module boundary violation). Export dedicated read-ports or service methods from `ChildModule` for the needed capabilities; inject those instead. Path: `src/modules/diagnostics/my-todos.service.ts:2`, `diagnostics.module.ts:27`. B22.
  - **Concern 1: admin-bypass-on-PATCH audit gap** — admin PATCH on `diagnostic_entries` / `progress_notes` spoofs `callerStaffMemberId` to bypass `assertAuthoredBy`; no `last_modified_by_user_id` + `last_modified_at` audit columns exist. Admin overrides are invisible in the DB. Future: add audit columns + service stamps them; surface in `endpoints.md` §3.10/§3.11. B22.
  - **Concern 2: empty-body 422 vs entity invariant layer mismatch** — `@IsNotEmpty()` on DTO fields fires 422 (class-validator) before domain `empty_body` error (400 entity invariant) can fire; the entity invariant is unreachable from HTTP. Harmless (still 4xx) but the invariant is dead from HTTP path. Pick one layer or document 422 explicitly in `endpoints.md` §3.10/§3.11. B22.
  - **Composite FK `(kindergarten_id, id)` on `children`** — T7 codex recommended adding a composite unique `(kindergarten_id, id)` on `children` so `diagnostic_entries.(kindergarten_id, child_id)` and `progress_notes.(kindergarten_id, child_id)` can reference it directly at DB level (defense complement to the service-layer `assertActiveChildInKg` guard). T8 added the service guard; the FK migration is deferred. B22 or dedicated DB hardening pass.

- **B17 T6/T7 review carry-forwards (MEDIUM/LOW)** — deferred to B22 polish:
  - **T6-MEDIUM#5 — `decodeURI` in `LocalFileStorageAdapter.assertSafeKey`** — user-supplied keys arriving via URL-encoded paths are not decoded before path-traversal check; a future UI that passes encoded slashes (`%2F`) could bypass the `..` guard. Add `decodeURI` + re-validate after decode for defense-in-depth. Path: `src/modules/content/infrastructure/local-file-storage.adapter.ts`. B22.
  - **T6-MEDIUM#6 — per-child SAVEPOINT in birthday batch** — `BirthdayGeneratorService` wraps the entire kg's batch in one TX; a single-child failure (e.g. template render error) aborts the whole batch and no birthday posts are created for that kg. Add per-child SAVEPOINT (TypeORM nested `manager.transaction()`) so a single failure is isolated and the batch continues. Path: `src/modules/content/birthday-generator.service.ts`. B22.
  - **T6-MEDIUM#8 — `menu_pub` / `schedule_pub` silent publish policy** — no notification is sent when a menu or schedule is published via `ContentPublishProcessor`. Document explicitly in `docs/Shyraq BP.md` §9.2/§9.3 that menu/schedule auto-publish is silent, OR add `notifyMenuPublished` + `notifySchedulePublished` emit calls. Product decision; B22.
  - **T6-MEDIUM#11 — `existsBirthdayForChildOnDate` SQL not index-optimally** — the `WHERE DATE(created_at AT TIME ZONE 'Asia/Almaty') = $date` predicate prevents the btree index on `created_at` from being used (non-IMMUTABLE function). Add a partial functional index or store a generated column `birthday_date` for efficient lookup if birthday volume grows. B22.
  - **T6-LOW#12 — env-example phase-A note for `s3`/`yandex` adapters** — `env-example-relational` entry `FILE_STORAGE_PROVIDER=local` should note that `s3` and `yandex` values will throw on bootstrap (stub adapters throw explicit errors) so developers don't accidentally set them in demo. B22.
  - **T6-LOW#13 — `mkdir` vs `writeFile` error discrimination in `LocalFileStorageAdapter`** — both filesystem errors currently bubble as generic 500. Distinguish `ENOENT` / `EACCES` / disk-full (`ENOSPC`) and map to meaningful log messages or 503 for disk-full. B22.
  - **T6-LOW#14 — Scenario M nanny test mislabel** — `test/content.e2e-spec.ts` Scenario M is described as "nanny cannot create story" but actually tests a non-guardian user (no `child_guardian` row); the nanny-specific restriction (role='nanny' policy) is not separately exercised. Add a dedicated nanny-positive sub-scenario. B22.
  - **T6-LOW#15 — `pickName(i18n)` empty-string fallback masks bad payloads** — if both `i18n.ru` and `i18n.kz` are empty strings (valid JSON but empty), `pickName` returns `''` without throwing; downstream rendering silently produces blank names in birthday content. Add `if (!name) throw new InvariantViolationError(...)` guard. B22.
  - **T6-LOW#16 — `ContentFeedService` 6 parallel queries could unify** — `ContentFeedService.getFeed()` issues 6 concurrent `repo.list(...)` queries (news + schedule_pub + menu_pub + birthday + group stories + kg-wide stories). Could be unified into a single `UNION ALL` query with `ORDER BY published_at DESC LIMIT N` for better performance under load. B22.
  - **T6-LOW#17 — `incrementViews` boolean ignored; stale 200 on race** — `StoryService.incrementViews` returns a boolean (`true` = incremented, `false` = already viewed or story not found) but the controller unconditionally returns 200. A `false` response on story-not-found should map to 404 instead of a stale 200. B22.
  - **T6-OBS#18 — dead `'media_urls' in dto` branch in update controller** — `AdminContentController.update` has an `if ('media_urls' in dto)` branch that is always entered when the DTO field is present (even when `undefined`), making the else branch dead code. Clean up the conditional or document the intent. B22.
  - **T6-OBS#20 — `kk` vs `kz` i18n key inconsistency** — some notification templates use `kk` (BCP 47 for Kazakh) and others use `kz` (country code, non-standard). Pre-existing across earlier batches, not B17-introduced. Standardise on `kk` (BCP 47 correct) in B22 i18n sweep.

### Resolved (исторические — full log в memory)

- **2026-05-07 · B16 T3 · close B13 LOW#2 — InvoiceService siblingsCount** — `InvoiceService` now passes `familyContext.siblingsInKgCount` to the engine via `DiscountEvaluationInput`; `MockDiscountEngine` accepts both legacy `context.siblingsCount` and new `context.familyContext.siblingsInKgCount` paths. Sibling-based discount now evaluates correctly on monthly cron + all invoice generation paths. Closed in `2d7d133`.

- **2026-05-03 · B11 T7-5 · external review #2 (3 HIGH + 2 MEDIUM + 1 LOW)** — HIGH#1: `validateOtp` now re-checks `trustedPerson.isAvailableForPickup()` after the advisory lock acquisition and BEFORE `attendance.checkOut`, closing the window where a parent revokes the trusted_person between create and validate-otp (parent revoke succeeded but staff could still validate the previously created pickup_request → broken revocation invariant). Throws `TrustedPersonRevokedError` (410) consistent with the create-time guard. HIGH#2: `TrustedPersonRepository.markUsed` grew a guarded UPDATE (`WHERE id=$1 AND used_at IS NULL AND revoked_at IS NULL AND is_active=true`) for the deactivate=true branch and now returns `boolean` (claim winner / loser); service throws `TrustedPersonRevokedError` if `claimed === false` on a `is_one_time` row, ambient TX rolls back attendance + status flip. Closes the concurrent-validate double-use of one-time trusted_people that the per-request advisory lock could not prevent (two pickup_requests sharing a tp row each held their own lock). For non-one-time rows the guard drops `used_at IS NULL` so the audit-style "last used" stamp behaves correctly. New `test/pickup-trusted-person.race.integration.spec.ts` proves 5 concurrent claims → exactly 1 winner, 4 losers, row consumed in DB. HIGH#3: trusted-people CRUD (add/update/revoke) gated by `permissions.effective(role).trusted_people_manage === true` (locked permission, primary-only by default per `docs/endpoints.md` §4.13); secondary/nanny → 403 `trusted_people_manage_required`. `listByChild` keeps the laxer "any approved guardian" check — secondary/nanny still see the whitelist. `assertCanManageTrustedPeople` helper added; old `assertCallerCanManage` → `assertParentGuardianLink` returns the guardian aggregate. MEDIUM#4: `NotificationDispatcher` recipient resolvers for `pickup.otp_sent` + `pickup.validated` now re-validate the requester via new `ChildGuardianRepository.findApprovedActiveByUserAndChild` port method (kg-scoped, no `can_pickup` requirement); revoked requester silently dropped from recipients (closes leak where parent revoke mid-flow still gets pickup notifications for the request's TTL window). MEDIUM#5: error contract synced — new module error `PickupOtpExpiredError extends GoneError` (410 `otp_expired`) now thrown by `validateOtp` on Redis-miss (was reusing auth's `OtpExpiredError` → 400 which contradicts `docs/endpoints.md` §3.6 contract); `sendOtp` on expired pickup_request now throws existing `PickupRequestExpiredError` (410) instead of `PickupRequestStatusInvalidError` (409). Pickup contract is now consistent with the SoT error map. LOW#6: `TrustedPersonRelationalRepository.listByChild` adds `WHERE is_active=true AND revoked_at IS NULL` — matches `docs/endpoints.md` §4.6 "Возвращает is_active=true записи" contract; revoked-history rows live in audit DB but never surface through the parent list endpoint. Cross-module: new `ChildGuardianRepository.findApprovedActiveByUserAndChild` port + relational impl + 5 fake stubs (pickup × 2 / dispatcher / attendance / child / auth / identity-qr / ws-auto-subscribe service-spec fakes). `endpoints.md` §3.6 validate-otp errors row updated: dropped stale `410 otp_invalid` (was inconsistent with T7-3 `400 invalid_otp` fix) + added `410 trusted_person_revoked` (HIGH#1 contract). Tests: unit 821→834 (+13: 4 new HIGH#3 tests in trusted-person spec + 2 listByChild secondary positive + 4 HIGH#1/#2/M#5 in pickup-request spec + 3 MEDIUM#4 dispatcher resolver tests); e2e 147→150 (+3: Scenarios Q/R/S — revoke-mid-flow, sequential one-time race, secondary 403; Scenario E updated to expect 410 `otp_expired`); race-integration 2→3 (`pickup-trusted-person.race.integration.spec.ts`). Build + lint + targeted e2e + race-integration all green; commit `bf7ae87`.

- **2026-05-02 · B11 T7-4 · post-finalize external review fixes (1 HIGH + 1 MEDIUM + 1 LOW)** — HIGH: snake_case DTO contract — B11 DTOs declared camelCase fields (`childId`, `trustedPersonId`, `fullName`, `photoUrl`, `isOneTime`, …) which `endpoints.md` §3.6/§4.6 documents as snake_case (`child_id`, `trusted_person_id`, `full_name`, `photo_url`, `is_one_time`, …); with `whitelist: true` global ValidationPipe documented requests would have been silently dropped, breaking the public contract (other modules' tests confirm convention: `auth.presenter.ts:16`, `test/children.e2e-spec.ts:42`, `test/attendance.e2e-spec.ts:63`). Fix: renamed DTO fields to snake_case (`AddTrustedPersonDto`, `UpdateTrustedPersonDto`, `PickupRequestResponseDto` already had snake_case wire keys), controllers map to camelCase service-layer types via local destructuring, e2e tests updated (request bodies snake_case to match). Two-DTO split for create endpoints: `StaffCreatePickupRequestDto` keeps `child_id` because staff endpoint has no URL `:id` param; `ParentCreatePickupRequestDto` drops `child_id` — pulled from URL `:id` (matches endpoints.md §4.6 contract). MEDIUM: sendOtp race с cancel/validate — sendOtp didn't acquire `acquireValidateAdvisoryLock`, opening race window where cancel/validate could flip status to terminal between sendOtp's status read and Redis/DB/SMS side-effects (worst case: SMS sent + outbox event emitted for cancelled/validated request). Fix: sendOtp acquires same advisory lock + re-fetches via `findByIdForUpdate` after lock; status guard re-applied under lock now blocks the side-effect leak. LOW: `otp_ref` (Redis key namespace `otp:pickup:{requestId}`) leaked in list/get/create/cancel responses — removed from `PickupRequestResponseDto` + `pickup.presenter.ts`; endpoints.md §3.6 row + JSON example synced. `SendPickupOtpResponseDto` keeps `otp_ref` (auditable info for calling staff per master plan §Pickup OTP flow). Tests: unit 821→823 (+2 sendOtp advisory lock tests), e2e 147 unchanged (all green); build+lint+test:e2e all green; commit `e760fac`.

- **2026-05-02 · B11 T7-3 · T7-2 second-opinion fixes (4 MEDIUM + 3 LOW)** — codex CLI fallback к opus reviewer (codex 1.0.4 default model gpt-5.5 unavailable; CLI upgrade attempted but cache-locked binary). NM1: TrustedPerson.revoke now throws TrustedPersonRevokedError (410) for already-revoked + not-active guards (was bare `throw new Error(...)` → silently 500). NM2: TrustedPersonService.update guards revoked rows (`if (tp.isRevoked() || !tp.isActive) throw 410`) — closes backdating-history attack. NM3: ListPickupRequestsQueryDto for `GET /staff/pickup-requests` (was bare `@Query` typed as TS-erased PickupRequestStatus → `status=garbage` triggered PG enum error → 500). NM4: endpoints.md §3.6 validate-otp response shape synced to actual richer impl `{ pickup_request: <full>, attendance_event_id }`. NL1: dropped unnecessary `forwardRef(() => AttendanceModule)` (no circular dep). NL2: Swagger validate-otp `@ApiUnprocessableEntityResponse` → `@ApiBadRequestResponse` (matches DomainErrorFilter 400 mapping). NL3: endpoints.md §3.6 error map synced — `invalid_otp` → 400 (was `otp_invalid` 410), `otp_rate_limit` → 429 (was `otp_rate_limited`). 4 OBS deferred (advisory lock before kg ownership check / DoS surface, list cross-tenant e2e gap, Redis flap notification gap (at-least-once acceptable), pickup_requests.attendance_event_id FK NO ACTION asymmetry — all minor; B22 polish). Tests: unit 819→821 (+2 entity + service tests for NM1+NM2), e2e unchanged. Closed in `795bf9c`.

- **2026-05-02 · B11 · Pickup OTP & Trusted Person (BP §7)** — Migration `1777788800000-B11PickupAndTrusted.ts`: 2 tables (`trusted_people`, `pickup_requests`) + ENUM `pickup_request_status` + FORCE RLS + tenant_isolation + indexes (`(child_id, is_active)`, `(kindergarten_id, child_id)`, `(kindergarten_id, status)`, `(child_id, created_at DESC)`); FK `attendance_events.pickup_request_id → pickup_requests.id` added as ALTER (B8 created column without FK); `attendance_method` ENUM already had `otp_pickup` (B8). PickupModule: `TrustedPerson` + `PickupRequest` POJO domain (state machine `otp_sent → {validated, expired, cancelled}`, `validate(staffMemberId, attendanceEventId, now)`, `cancel(now)`, `expire(now)`, `revoke(now)`, `markUsed(now, deactivate)`). 7 module errors via shared-kernel base classes (NotFoundError/GoneError/ConflictError/ForbiddenActionError). Services: `pickup-request.service.ts` (createByStaff/createByParent/sendOtp/validateOtp/cancel/list/get) + `trusted-person.service.ts` (parent CRUD list/add/update/revoke). 3 controllers under `/staff/pickup-requests/*`, `/parent/children/:id/{trusted-people,pickup-requests}`, `/parent/trusted-people/:id`. Pickup OTP flow: `OtpStorePort` (auth's per-phone budget reused via AuthModule export — single rate-limit quota for login + pickup), per-request keyed `PickupOtpStorePort` (Redis `otp:pickup:{requestId}` TTL 1800s + standalone attempts counter `otp:pickup:{id}:attempts` INCR atomic). Advisory lock `pg_advisory_xact_lock(hashtext('pickup:validate:'||requestId))` via `PickupRequestRepository.acquireValidateAdvisoryLock` port method (CLAUDE.md §8 boundary maintained); validate-otp serializes via lock + SELECT FOR UPDATE; cancel acquires same lock + uses `update(... { expectedStatus: 'otp_sent' })` for defense-in-depth race protection. ValidateOtp ordering after T7 M3: status/expiry checks → isLocked check → rate-limit → readCode/match → attendance.checkOut(...) (ambient TX, method='otp_pickup', skip pickup-guardian validation when pickup_request validated) → pickupRequests.update(validated) → trustedPeople.markUsed if applicable → clearCode (after DB writes — replay protection via state machine guard) → notifyPickupValidated. Cross-module: `attendance.service.checkOut(...)` widened with optional `pickupRequestId`/`method` (backward compat preserved); `NotificationDispatcher` event templates + recipient resolvers for `pickup.otp_sent` (push to requester) + `pickup.validated` (push to child guardians + requester); nanny-policy whitelist updated to include `pickup.*`. Sms templates: `pickupOtpTemplate(code, childName, kindergartenName)` (Russian, B14+ i18n deferred). Trusted-people whitelist: `is_one_time=true` rows auto-deactivate on first use (`is_active=false, used_at=now`). T6 opus code-review (1 CRITICAL / 5 HIGH / 6 MEDIUM / 3 LOW / 4 OBS): C1 route shadow `StaffController @Get(':id')` под `staff/` перехватывал `/staff/pickup-requests` (Express registration order); H1 race-integration spec dead (passed null repos triggering TypeError before reaching advisory lock); H2 inner `dataSource.transaction(...)` in validateOtp wasteful + misleading; H3 cancel-vs-validate lost-update flip (cancel didn't acquire advisory lock + repo update lacked status guard); H5 cancel skipped clearCode when otp_ref null (defensive coding); M2 child lookup hard-error vs kg silent fallback (UX inconsistency); M3 clearCode before DB writes silent loss on transient checkOut failure; M4 rate-limit consumed before isLocked check (cross-flow griefing on shared per-phone budget); M5 addedByUserId short-circuit allowed ex-guardian to manage trusted-people; M6 dead namespace fragment for attempts counter. T7 fix-pass `61a6853` closed C1 (StaffController moved 'staff' → 'admin/staff' to align with `endpoints.md` §2.2 SoT — was impl-vs-doc divergence, not docs change), H1 (real PG-backed race spec with FakeAttendanceService asserting exactly-1-validated + exactly-1-attendance-row across 3 concurrent callers), H2 (dropped wrapper, drop dataSource injection), H3 (cancel acquires lock + uses findByIdForUpdate; repo `update()` grew optional `expectedStatus` returning boolean → service maps 0-rows to PickupRequestStatusInvalidError), H5 (unconditional clearCode), M2 (child + kg both best-effort), M3 (clearCode after DB writes), M4 (isLocked before rate-limit), M5 (assertCallerCanManage drops short-circuit, only guardian-link authoritative), M6 (split standalone attempts key). H4 (phone PII on staff list) deferred to §5 Active — product/compliance decision. T7-2 (codex fallback opus) added 4 MEDIUM + 3 LOW; T7-3 closed all (see entry above). Test counts: unit 736→821 (+85); e2e 134→147 (+13: pickup.e2e-spec.ts Scenarios A–L, organization.e2e-spec.ts paths updated for C1 fix); race-integration 1→2 (`pickup.race.integration.spec.ts` real PG advisory-lock-serialization). Commit set (in order): T0 `5e47ed9` selectRole bypass_rls fix + parent flow e2e (closed §5 Active entry); T1 `2833d72` doc-sync; T2 `2365f4f` migration; T3 `031373c` domain+ports+adapters+module skeleton; T4 `de67024` services+controllers+DTOs+specs+cross-module; T5 `b945304` e2e Scenarios A–K + race-integration; T7 `61a6853` T6 fix-pass; T7-3 `795bf9c` T7-2 fix-pass. **B11 demo-ready — BP §7 closed.** Closes the last BP-block needed for parity with feature spec; demo flow: parent adds trusted_person via Parent App → staff in Staff App creates pickup_request when trusted person arrives → staff hits send-otp → trusted person receives SMS code → staff enters code → server validates and atomically writes attendance check-out + timeline entry + status='validated' + notifies guardians.

- **2026-05-02 · B11 T0 · selectRole RLS bug auto-resolved** — `POST /auth/role/select` parent path was failing with `new row violates row-level security policy for table "refresh_tokens"` (500). Root cause: `KindergartenScopeGuard` is a global guard and sets `req.tenant = { kgId: null, bypass: false }` for the `pending_role_select` JWT (no `kindergarten_id` claim). `TenantContextInterceptor` then opens a transaction and stores its manager in `tenantStorage` without setting any GUC (neither `app.kindergarten_id` nor `app.bypass_rls`). `RefreshTokenRelationalRepository.create` finds `tenantStorage.getStore()?.entityManager != null`, takes the `if`-branch, and the INSERT fails RLS `WITH CHECK`. Fix: `selectRole` in `auth.service.ts` now opens its own `dataSource.transaction` with `SET LOCAL app.bypass_rls = 'true'` and overrides `tenantStorage` via `tenantStorage.run({ ..., bypass: true, entityManager: manager }, ...)` before calling `refreshTokens.create` — identical pattern to `autoApprovePendingPrimaries`. Confirmed by new e2e `test/auth-select-role-parent.e2e-spec.ts` Scenario P (parent with guardian links in 2 kgs → `pending_role_select: true` → `selectRole` → 200 + rotatable refresh token). Build + lint + unit + targeted e2e all green.

- **2026-05-02 · B10 T7-2 · Codex second-opinion fixes (4 findings)** — HIGH#1: reuse-flow restored on GET /users/me/qr via paired Redis key `qr:user:{userId}:identity → plaintext` (TTL = token TTL); reuse when active row + plaintext present + expires_at-now > 1h; mint+revoke otherwise. Admin revoke-all now also `DEL`s the user-keyed cache key (next user GET mints fresh; `qr:token:{plaintext}` still relies on natural TTL + DB-recheck). Closes contract-vs-impl divergence introduced silently in T4 ("always-mint" was never approved). HIGH#2: removed `SET LOCAL app.bypass_rls='true'` GUC leak from `RefreshTokenRelationalRepository.hasActiveSessionForDevice` (PG `SET LOCAL` not reset by `RELEASE SAVEPOINT` — leaked into ambient request TX). Verified bypass unnecessary by tracing OTP-verify: refresh_token's `kindergarten_id` matches caller's GUC; plain query through `manager()` passes RLS naturally. MEDIUM#1: admin POST /admin/qr/revoke-all/:userId now scoped to caller's kg — target user must be active staff_member of caller's kg OR approved+active child_guardian for child in caller's kg, else 403 `user_no_relationship_to_kg`; missing user → 404 `user_not_found` (closes prior `revokedCount: 0` masking + cross-tenant privilege escalation). Reused existing `StaffMemberRepository.findActiveByUserAndKindergarten` and `ChildGuardianRepository.findApprovedActiveByUserIdCrossTenant` with optional kg filter. MEDIUM#2: moved `pg_advisory_xact_lock` from inline service SQL to `IdentityQrRepository.acquireUserAdvisoryLock(userId)` port method — service no longer imports `tenantStorage` or runs raw SQL (CLAUDE.md §8 boundary restored). E2E: scenario B rewritten ("re-GET within 1h returns SAME token"); scenario C rewritten ("re-GET when <1h to expiry mints+revokes"); new scenario L (admin revoke clears reuse-cache); 2 new admin scenarios (cross-tenant 403 + missing-user 404). Race-integration: 3 concurrent GETs → 1 active row + 0 revoked + all callers share plaintext (advisory-lock + reuse on cache hit). Tests: unit 730→736; e2e 122→134; race-integration green (real PG). Commits `454f235` → `cbca0da` → `18281df`.

- **2026-05-02 · B10 · Identity QR (BP §13)** — Migration `1777688591994-B10IdentityQr.ts` creates `user_qr_tokens` table + `qr_purpose` ENUM; NO RLS (cross-tenant by design — parent with children in multiple kindergartens uses one QR). Partial unique idx `(user_id, purpose) WHERE revoked_at IS NULL` (without `NOW()` predicate — not IMMUTABLE; one-active invariant enforced in service TX logic). IdentityQrModule: `QrToken` POJO domain entity with full state machine (`isActive`, `isExpired`, `shouldRefresh`, `revoke`, `recordScan`); 4 module errors (`QrTokenNotFound` → 404, `QrTokenExpired`/`QrTokenRevoked` → 410, `QrScanRateLimitExceeded` → 429). Two new shared-kernel base errors `GoneError` (410) + `TooManyRequestsError` (429) wired into `DomainErrorFilter`. Token model: `crypto.randomBytes(16).toString('hex')` opaque 32-hex, SHA-256 hash in DB `token_hash`, plaintext-only in Redis `qr:token:{plaintext}` TTL 24h. Ports: `QrTokenCachePort` / `RedisQrTokenCacheAdapter` + `QrScanRateLimiterPort` / `RedisQrScanRateLimiterAdapter` (`rl:qr:scan:{device_id}` INCR+EXPIRE 60). Three endpoints: `GET /users/me/qr` (lazy-issue, every GET always issues fresh / auto-refresh; no reuse branch), `POST /staff/qr/scan` (rate-limit 60/min/device, `X-Device-Id` validated against active refresh-token session, `linkedChildren` kg-scoped to scanning staff), `POST /admin/qr/revoke-all/:userId` (DB bulk-revoke; Redis DEL not feasible for all plaintexts — relies on DB-recheck on next scan). Cross-module wiring added: `RefreshTokenRepository.hasActiveSessionForDevice`, `ChildRepository.findByIdsCrossTenant`, `ChildGuardianRepository.findApprovedActiveByUserIdCrossTenant`. T6 opus code-review (3 CRITICAL / 4 HIGH / 4 MEDIUM / 4 LOW / 4 OBS): C1 X-Device-Id not plumbed through auth endpoints; C2 concurrent `GET /users/me/qr` 23505 race; C3 `linkedChildren` cross-tenant leak. T7 closed all 3 CRITICAL across 3 commits: `6f80bd0` plumbed `X-Device-Id` header through `/auth/otp/verify`, `/auth/refresh`, `/auth/role/select` response + removed stale helper; `f46a06a` advisory lock `pg_advisory_xact_lock(hashtext('qr:identity:'||userId))` on `issueOrRefresh` + dropped dead inner `dataSource.transaction()` (H2) + added advisory-lock integration race spec; `75c27b0` Variant A kg-scoped `linkedChildren` (C3) + new e2e Scenario K (parent with kids in two kgs → only scanning-kg's child returned) + doc updates (H1 admin-revoke Redis behaviour in endpoints.md, M4 DTO comment fix, L1 staffMembers TODO marker). T7 also found: `selectRole` RLS bug for parent path (pre-existing, deferred to B22, tracked in §5 Active); advisory-lock no-ambient-TX caveat (tracked in §5 Active). Tests post-T7: unit 59 suites (non-gated) / 730 tests; e2e 17 suites / 131 tests (+1 suite +12 tests: `identity-qr.e2e-spec.ts` Scenarios A–K); integration `identity-qr.race.integration.spec.ts` green under `INTEGRATION_DB=1` (env-level `password authentication failed` tolerated across all integration suites — pre-existing, same as B9). Commit set `661111d` → `75c27b0` (8 commits): T1 sonnet `661111d` docs; T2 sonnet `e85698f` migration; T3 opus `949937c` domain+ports+adapters+shared-kernel errors; T4 opus `d9d9dd5` service+controllers+DTOs+Swagger; T5 sonnet `2105961` e2e A-J; T6 opus review (no commit); T7: `6f80bd0` C1 fix, `f46a06a` C2 fix+H2, `75c27b0` C3+Scenario K+H1/M4/L1. Closed in `75c27b0`.

- **2026-05-02 · B9 follow-up review** — closed 5 follow-up items from B9 R10 observations: (1) `dashboardAttendanceToday` migrated to `ClockPort` for test determinism (`20c9ec6`); (2) `NotificationDispatcher` EVENT_TEMPLATES + RECIPIENT_RESOLVERS now cover `guardian.pending_approval/rejected/revoked`, `child.transferred`, `guardian.permissions_updated`; new `push-error-classifier.ts` splits push errors into permanent (token deleteById on FCM NotRegistered/InvalidRegistration/MismatchSenderId, APNS BadDeviceToken/Unregistered/DeviceTokenNotForTopic) vs transient (markFailed for retry) (`8245f75`); (3) `push_tokens` UNIQUE migrated from `(user_id, token)` to `(platform, token)` globally + atomic ownership-transfer via `ON CONFLICT` — closes ownership-leak on shared device (`75b7936`); (4) `WeeklyRolloutProcessor` errors no longer swallowed (rethrow + BullMQ 3×exp-backoff 1m/2m/4m); `WsAutoSubscribeService` rooms scoped by JWT `role` + `kindergarten_id` on handshake — cross-tenant rooms in OTHER kgs excluded (`52e738d`); (5) `docs/schema.dbml` + `endpoints.md` + `architecture.md` synced with actual B9 implementation (push_tokens columns `device_id`+`last_seen_at`, notifications NOT NULL `title_i18n`/`body_i18n`/`data`, `ws_enabled`→`in_app_enabled` in preferences, WS `auth_error` event, JWT-scoped auto-subscribe rooms) (`ffda783`). Closed across `20c9ec6` → `ffda783`.

- **2026-05-01 · B9 · Notifications + WebSocket** — Outbox-pattern table `notification_outbox` (FORCE RLS, `idx_outbox_pending` partial idx) writes inside business TX. BullMQ worker process (`src/main.worker.ts`) drains via `OutboxPollerProcessor` every 2s with `FOR UPDATE SKIP LOCKED` claim + per-event savepoint isolation. `NotificationDispatcher` resolves recipients (guardians for child-events, mentor for group-events) + applies `notification_preferences` + nanny-policy filter, fans out to push (`PushNotificationPort` + `MockPushAdapter`, `FcmPushAdapter` stub for B22) + WS rooms + history rows in `notifications` table. `NotificationGateway` @ `/ws` with handshake JWT auth (`auth.token`, not query) + auto-subscribe `user:{id}` + `child:{cid}` (approved guardians cross-tenant) + `group:{gid}` (active mentors cross-tenant). `RedisIoAdapter` for cross-process pub/sub (api ↔ worker). REST: `POST/DELETE /push-tokens`, `GET /notifications` (cursor-paged), `POST /:id/read` + `/read-all`, `GET/PATCH /notifications/preferences`. T8 closed all `// TODO(B9)` sites: synchronous `await notifyXyz(...)` inside ambient TX (atomic with business mutation), `notifyGuardianApproved` emit added in `autoApprovePendingPrimaries`, `notifyGuardianSelfRevoked` emit in self-revoke. T9 surprise: `OutboxPollerProcessor` missing `tenantStorage` propagation — silently RLS-dropped notifications — fixed inline. T10 opus review: 1 CRITICAL (dispatcher DB error poisoning entire worker batch → duplicate delivery) + 1 MEDIUM (preferences PATCH race + first-insert default leak) + 1 LOW + 8 OBS. T11 fix-pass `e704acd` closed CRITICAL via per-event savepoint in `OutboxPollerProcessor` and MEDIUM via single-statement `INSERT … ON CONFLICT DO UPDATE` with EXCLUDED merge. Migration `1777627742228-B9NotificationsAndOutbox.ts` (4 tables). Tests: unit 51→56 suites / 569→636 tests; e2e 14→16 suites / 111→119 tests. Closed in `e704acd`.

- **2026-05-01 · B9 · autoApprovePendingPrimaries без notify (B6 leftover)** — `notifyGuardianApproved` emitted synchronously inside per-kg TX in `autoApprovePendingPrimaries` (T8). Resolved.

- **2026-05-01 · B9 · schedule:weekly-rollout cron lock (B7 T5 leftover)** — `weekly-rollout.cron.ts` deleted; `WeeklyRolloutProcessor` migrated to BullMQ repeatable-job with Redis-based distributed lock free via `BZPOPMIN` (T6). Resolved.

- **2026-05-01 · B9 · NotificationPort post-commit microtask vs afterCommit (B8 leftover)** — all `fireAndForget` wrappers replaced with synchronous `await notifyXyz(...)` inside the same ambient TX; `OutboxNotificationAdapter` writes atomically via `tenantStorage` `EntityManager` (T8). Resolved.

- **2026-05-01 · B8 · Attendance & Timeline (manual)** — manual check-in/out, timeline CRUD, daily-status. Atomic 3-table TX inside ambient interceptor TX. Pickup validation via approved guardian + can_pickup. Edit window same-day Asia/Almaty for non-admin staff. NotificationPort +4 methods (logging adapter + B9 WS TODO). Migration `1777588264314-B8AttendanceAndTimeline.ts`. T6 opus review caught 0 CRITICAL + 2 HIGH (admin filters, dashboard groupId) + 4 MEDIUM; T7 fix-pass `05cddc2` closed all. Closed in `05cddc2`.

- **2026-04-30 · B7 · Cron-механизм для weekly auto-copy** — выбран `@nestjs/schedule` (`@Cron('0 23 * * 0', { timeZone: 'Asia/Almaty' })`), реализован в `WeeklyRolloutService`. Идемпотентность через `existsAnyInRange` short-circuit (meal) + tryCreate snapshot-first ordering (schedule). Distributed lock (`// TODO(B9): add cron lock via Redis SET NX EX`) отложен до B9 (горизонтальное масштабирование). Закрыт в `b27b5dc`.

- **2026-04-30 · B6 · Parent Onboarding** — link/unlink endpoints implemented (`POST /parent/children/link`, `POST /parent/children/:id/unlink`), auto-approve primary в `verifyOtp` (defer from B5 — closed). Parent JWT scope (P5 leftover) — closed via `6b0a929` и exercised by Scenario A e2e. ForbiddenActionError base введён в shared-kernel; DomainErrorFilter now passes through `details` for clients (used by `multiple_children_for_iin`). No DB migration needed.

- **2026-04-30 · B5 · Enrollment** — invoice generation на `card_created` — defer на B13. Hook-point: `EnrollmentService.transition()` где помечен `// TODO(B13)` маркер. Auto-approve primary guardian по phone — defer на B6 (parent linking flow).

- **2026-04-26 · D14 · Process split по execution profile (api / worker / ws)** — на B9 разделяем cloud-процессы. До B9 — single-process. Не разделяем по user-role (admin/parent/super-admin same code).
- **2026-04-26 · D15 · Cloud + Edge topology** — Face/CCTV stack on-premise per kindergarten (mini-PC ~$400 docker-compose). Cloud-БД никогда не содержит face embeddings. Outbound-only WSS edge→cloud, mTLS, command/event pattern. На B18.5 — `edge_commands` / `edge_health` / `kindergarten_edge_credentials`. На B19 — `face_consents` (только согласие, не биометрия).

Полный лог архитектурных решений (D1–D16, P0–P5) — в migration memory `C:\Users\Doszhan\.claude\projects\c--Users-Doszhan-Desktop-work-projects-shyraq-app-backend-shyraq-v2\memory\project_v2_migration.md`.
